# FastAPI 백엔드 — Phase 1 Slice 5 의존성
# 추가 의존성은 후속 Slice 진입 시점에 추가 (Surgical Scope)

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Validation / settings
pydantic==2.9.2
pydantic-settings==2.5.2

# LLM
openai==1.51.0

# Phase 11 A안 Slice 1: Google Gemini (cross_validation, gated). additive — graceful
# (gemini_adapter SDK import 지연 → 미설치 환경에서도 모듈 import 성공).
google-genai>=1.0

# Phase 11 B안 Slice 1: Anthropic Claude (3-provider, gated). additive — graceful
# (anthropic_adapter SDK import 지연 → 미설치 환경에서도 모듈 import 성공).
anthropic>=0.40

# Env
python-dotenv==1.0.1

# Slice 4: pgvector + Postgres (graceful fallback이므로 install 미설치도 동작)
psycopg[binary]>=3.2
pgvector>=0.3

# Slice 5: Supabase (graceful — env 미설정 또는 미설치 시 DB 저장 자동 skip)
supabase>=2.7

# Test
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
