# FastAPI 백엔드 — Phase 1 Slice 1 의존성
# 추가 의존성은 후속 Slice 진입 시점에 추가 (Surgical Scope)

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Validation / settings
pydantic==2.9.2
pydantic-settings==2.5.2

# LLM
openai==1.51.0

# Env
python-dotenv==1.0.1

# Test
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Note: Slice 4 추가 예정 — psycopg[binary], pgvector
# Note: Slice 5 추가 예정 — supabase
