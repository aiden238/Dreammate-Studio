# Phase 17 다-S4 — 개인 PKM 전체 루프 e2e (라이브, 실 LLM)

> 날짜: 2026-06-04 | flag ON(PERSONAL_PKM_EXTRACT/INJECTION) + 실 LLM(gpt-4o-mini, director)
> 러너: `Temp/run_pkm_e2e.py`(gitignored) — DI 로 PkmRepo 공유(in-process, dev mock in-memory 휘발 회피)
> 목적: feedback→추출→pkm_entries→다음 생성 주입 전체 루프 실동작 입증(P-LIVE-VERIFY-001)

## 결과 = PASS

**① 추출** — feedback(명시 reason 2건) → `_run_personal_pkm_extract_hook` → pkm_entries **4건 적재(전부 conf 0.9)**:
- `avoid_phrase` (0.9): "과장된 표현이 부담스러워요"
- `preferred_phrase` (0.9): "담백하고 솔직한 톤이 좋아요"
- `rejection_pattern` / `success_pattern` (0.9)

**② 주입** — 같은 `auth_user_id` 로 `generate_plan`(실 LLM, pkm_repo 공유):
- 생성 plan[0] **톤 = "담백하고 솔직한 톤"** (적재된 선호 그대로)
- 컨셉 = "동네 카페 신메뉴 원두의 매력을 담백하게 소개"
- 담백·솔직 반영 = **True** / 과장 회피(미등장) = **True**

## 의미

```
feedback → 추출(≥0.9) → pkm_entries → 다음 생성 시 구속 주입 → plan 이 학습된 선호 반영.
= "쓸수록 맞춰지는" agent-grade 동작을 실 LLM 닫힌 루프로 입증.
```
가-S2(brand 주입 라이브) + 다-S6(추출 wiring unit) + 본 e2e(추출→주입 합성) → 개인 PKM 루프 전 구간 검증.

## 비고

- RAG 임베딩 단계는 graceful skip(`embedding_failed ... graceful`) — PKM 루프와 무관, 영향 0.
- dev mock in-memory 는 요청 간 휘발 → 본 e2e 는 DI 로 동일 PkmRepo 인스턴스 공유(in-process)로 루프 입증. 실 HTTP 다요청 e2e 는 Supabase 영속 필요(배포 게이트).
- behavior-preserving: flag 는 이 e2e 한정 env ON(.env/커밋 불변, default OFF 유지).
