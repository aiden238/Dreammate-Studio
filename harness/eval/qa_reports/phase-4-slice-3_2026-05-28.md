# QA Check Report — Phase 4 Slice 3

> Type: slice-completion gate
> Slice: 3 / 4 (Frontend 3-plan minimal — `/plan/[plan_id]` PlanCard × 3 세로 스택 + 1 선택)
> 실행일: 2026-05-28
> 결과: **ALL PASS** (tsc 0 / lint 0 / build 0 / pytest 93/93 / audit_naming 0 / audit_page_component 0 (D-1 deviations 기록) / §SELF-VERIFICATION PASS)

---

## 1. 구현 Eval (Implementation Track)

### 1.1 자동 테스트 결과

```
npx tsc --noEmit:    0 errors
npx next lint:       ✔ No ESLint warnings or errors
npx next build:      ✓ Compiled successfully — 11 routes
                       Route (app)                       Size     First Load JS
                       ├ ○ /plan                         11.3 kB  98.4 kB     (Phase 1 보존)
                       └ ƒ /plan/[plan_id]               3.69 kB  95.9 kB     (★ Slice 3 신규)

pytest -q:           93 passed, 3 warnings in 1.11s
                       = Phase 4 Slice 1 baseline 77 + Slice 2 신규 16 = 93
                       회귀 0 (frontend-only 변경 → 백엔드 무영향)

audit_naming.ps1:    PASS (0 drift)
                       plan_candidates    PASS  0
                       video_projects     PASS  0
                       critic_evaluation  PASS  0
                       rag_references     PASS  0

audit_page_component.ps1:
  routes:    11 actual / 14 spec — `/plan/[plan_id]` actual only
             → deviations.md D-1 기록 (intended drift — scope.md ⊃ page_map.md)
             → Slice 4 에서 audit 스크립트 dynamic-route 정규화 보강 권장
  components: 9 actual / 45 spec — Phase 4+ deferred placeholder 정상

  → component drift 0 (신규 component 0건 — PlanCard 무수정 import만)
  → route drift 1 hit (intended, deviations.md 기록)
```

### 1.2 Acceptance 매핑 (acceptance.md A4 + A5 + A6)

| 작업 항목 | 산출 위치 | 결과 |
|---|---|---|
| `/plan/[plan_id]` dynamic route 페이지 | `apps/web/app/plan/[plan_id]/page.tsx` (NEW) | PASS |
| GET `/plans/{id}` 호출 + envelope null 시 POST `/generate` | `load()` callback | PASS |
| 3-plan PlanCard × 3 세로 스택 | `plans.map((plan, i) => <PlanCard plan={plan} />)` | PASS |
| 카드 클릭 → selected state + sessionStorage 저장 | `handleSelect()` | PASS |
| Loading 상태 (ProgressStepper 재사용) | `ProgressStepper currentStep="planning"` | PASS |
| Error 상태 (ErrorCard 재사용) | `ErrorCard error={error} onRetry={load} onHome={...}` | PASS |
| Phase 4 fetch wrappers (`startPlan` / `wizardStep` / `generateMultiPlan` / `getPlan`) | `apps/web/lib/api.ts` UPDATE | PASS |
| Phase 4 타입 (`MultiPlanEnvelope`, `PlanResource`, `PlanStartResponse`, `WizardStepResponse`) | `apps/web/lib/types.ts` UPDATE | PASS |
| Phase 1 `/plan` 페이지 보존 (sessionStorage envelope) | `apps/web/app/plan/page.tsx` (Phase 1 동작 그대로) | PASS |
| Phase 4 query `?plan_id=` 감지 시 redirect | `apps/web/app/plan/page.tsx` useEffect 최소 추가 | PASS |
| **PlanCard.tsx 무수정** ★ (사용자 결정 6-a) | `git diff HEAD -- PlanCard.tsx` empty | PASS |
| Phase 3 `/new/*` routes 회귀 0 (A5) | `git diff HEAD -- apps/web/app/new` empty | PASS |
| Phase 1 `/plan` 회귀 0 (A4) | Phase 1 동작 유지 + redirect 최소 추가만 | PASS |

### 1.3 정적 검증

- tsc 0 errors / lint 0 warnings / build 0 errors
- pytest 93/93 PASS (회귀 0 — frontend-only 변경)
- audit_naming 0 drift (4 canonical 모두 OK)
- audit_page_component: route drift 1 hit (deviations.md D-1 기록 — intended)

### 1.4 qa-check v1.2.0 적용 카테고리

| 카테고리 | 항목 | 결과 |
|---|---|---|
| 1. Scope / non-goals 정합 | PlanCard 4-layer 재정의 0 / PlanComparisonCard 신규 0 / SSE / Rewriter 0 | PASS |
| 4. UX (모바일 우선) | min-h-[44px] CTA / max-w-2xl / sticky bottom CTA / focus-visible | PASS |
| 10. 회귀 안전성 | Phase 1 `/plan` 회귀 0 + Phase 3 `/new/*` 회귀 0 + 백엔드 회귀 0 | PASS |
| 11. 산출물 정합 | 2 신규 (page + QA) + 3 수정 (api/types/plan) + 1 deviations, forbidden 0건 | PASS |

(카테고리 2~3, 5~9는 Slice 4 영역 — smoke / archive / cost / golden_set 회귀)

### 1.5 Simplicity Score (5/5)

| 항목 | 점수 | 사유 |
|---|---|---|
| PlanCard 무수정 + 단순 `× N` 반복 | 5 | PlanComparisonCard 본격은 Phase 5+ 이관 (D4 deferred) |
| Phase 1 ErrorCard / ProgressStepper 재사용 | 5 | 신규 component 0건, fetch wrapper만 추가 |
| getPlan → null 이면 generate 호출 단순 fallback | 5 | 별도 state machine 없음, 1 callback 단순 흐름 |
| selected state — sessionStorage + visual ring | 5 | global store 도입 없이 useState + visual ring로 단순화 |
| CTA "이 기획안으로 진행" → 단순 alert (Phase 5+ 본격) | 5 | 피드백 흐름 / 저장 / Brand Memory 추출은 Phase 5+ 이관 |

---

## 2. 산출물 (2 신규 + 3 수정 + 1 deviations)

### 신규 (2)

| 파일 | 줄수 | 역할 |
|---|---|---|
| `apps/web/app/plan/[plan_id]/page.tsx` | ~265 | Phase 4 결과 페이지 — getPlan → envelope null 시 generateMultiPlan → 3 PlanCard 세로 스택 + 카드 클릭 selected state |
| `eval/qa_reports/phase-4-slice-3_2026-05-28.md` | (이 파일) | Slice 3 QA 게이트 |

### 수정 (3)

| 파일 | 변경 | 사유 |
|---|---|---|
| `apps/web/lib/api.ts` | +~226 / -0 | Phase 4 fetch wrappers 4종 (`startPlan` / `wizardStep` / `generateMultiPlan` / `getPlan`) + `GenerateMultiPlanResult` union 타입. 기존 `generate()` 무수정 보존. |
| `apps/web/lib/types.ts` | +~50 / -0 | Phase 4 타입 추가 (`MultiPlanEnvelope` (=Envelope alias), `PlanResource`, `PlanStartResponse`, `WizardStepResponse`). 기존 Phase 1 타입 무수정. |
| `apps/web/app/plan/page.tsx` | +14 / -0 | Phase 4 query `?plan_id=xxx` 감지 시 `/plan/[plan_id]` redirect. Phase 1 sessionStorage envelope 흐름 보존. eslint-disable-next-line 추가 (exhaustive-deps 의도적 유지). |

### Deviations 기록 (1)

| 파일 | 사유 |
|---|---|
| `phases/active/phase-4-fastapi-extension/deviations.md` | D-1: `/plan/[plan_id]` dynamic route 가 scope.md §3 명시 산출물이나 `page_map.md` 미반영 (intended drift). Slice 4 또는 다음 phase 의 contract-change 로 처리. |

---

## 3. §SELF-VERIFICATION (P-X1) — **8연속 PASS**

> Phase 3 5연속 (Slice 1~5) + Phase 4 Slice 1 + Slice 2 + **Slice 3** = **8연속 PASS streak**.

### 3.1 git status + git diff --stat HEAD 검증

```
Changes not staged for commit:
  modified:   harness/apps/web/app/plan/page.tsx           +14 / -0
  modified:   harness/apps/web/lib/api.ts                  +226 / -0
  modified:   harness/apps/web/lib/types.ts                +50 / -0

Untracked files:
  harness/apps/web/app/plan/[plan_id]/page.tsx             (NEW ~265 lines)
  harness/phases/active/phase-4-fastapi-extension/deviations.md
  harness/eval/qa_reports/phase-4-slice-3_2026-05-28.md
```

editable 외 forbidden 0건 확인.

### 3.2 Forbidden 0줄 PASS 표

| 항목 | 영역 | 결과 |
|---|---|---|
| **`apps/web/component_map.md`** ★ | 조정 4번 (**14연속** 시작) | 0줄 PASS |
| **`apps/web/components/PlanCard.tsx`** ★ | 사용자 결정 6-a (**3연속** 시작) | 0줄 PASS |
| `apps/web/page_map.md` | Phase 2 spec (Slice 3 read-only) | 0줄 PASS |
| `apps/web/design_handoff.md` | Phase 2 spec | 0줄 PASS |
| `apps/web/design.md` | Phase 2 spec | 0줄 PASS |
| `apps/web/design_system/*` | Phase 2 spec | 0줄 PASS |
| `apps/web/*flow.md`, `mode_branching.md`, `direction_approval.md`, `wireframes/*` | Phase 2 spec | 0줄 PASS |
| `apps/web/components/discovery/*` | Phase 3 stable | 0줄 PASS |
| `apps/web/components/common/DirectionApprovalCard.tsx` | Phase 3 stable | 0줄 PASS |
| `apps/web/components/quick/QuickInputCard.tsx` | Phase 3 stable | 0줄 PASS |
| `apps/web/components/{ErrorCard, ProgressStepper, SubmitButton}.tsx` | Phase 1 stable (import만) | 0줄 PASS |
| `apps/web/app/new/*` | Phase 3 stable (Discovery 7-step + Quick 4-step) | 0줄 PASS |
| `apps/web/app/page.tsx` | Phase 1 home (보존) | 0줄 PASS |
| `apps/web/lib/{state/wizard, discovery_state, quick_state, mode_branching, errors, design_tokens}.ts` | Phase 3 stable | 0줄 PASS |
| `backend/fastapi/` 전체 | Slice 1/2 stable | 0줄 PASS |
| `scripts/smoke_test_phase_4.ps1` | Slice 4 영역 | 0줄 PASS (파일 자체 없음) |
| `eval/qa_reports/phase-4-final*.md` | Slice 4 영역 | 0줄 PASS (파일 자체 없음) |
| `meta/retrospectives/phase-4.md` | Slice 4 영역 | 0줄 PASS (파일 자체 없음) |
| `harness/PROJECT_STATE.md`, `PHASE_REGISTRY.md` | Slice 4 갱신 영역 | 0줄 PASS |
| `phases/active/phase-4-fastapi-extension/*.md` (deviations.md 제외) | Slice 4 영역 | 0줄 PASS |
| `docs/contracts/`, `docs/decisions/`, `ai_system/`, `knowledge/`, `product/`, `meta/`, `.claude/skills/` | read-only | 0줄 PASS |

### 3.3 판정

**PASS** — editable 외 forbidden 0줄 + 회귀 0 + intended drift는 deviations.md 기록.

---

## 4. component_map.md 0줄 14연속 + PlanCard.tsx 0줄 3연속

### 4.1 `apps/web/component_map.md` — **14연속** 0줄 ★

- Phase 2 Slice 1~6: 6연속
- Phase 3 Slice 1~5: 5연속 (누계 11)
- Phase 4 Slice 1 + Slice 2 + **Slice 3**: 3연속 (누계 **14**)

`git diff HEAD -- apps/web/component_map.md` → empty.

### 4.2 `apps/web/components/PlanCard.tsx` — **3연속** 0줄 ★ (사용자 결정 6-a)

- Phase 4 Slice 1: 1연속
- Phase 4 Slice 2: 2연속
- Phase 4 **Slice 3**: **3연속** ★ (실제 import 사용 시점에서도 무수정 유지)

`git diff HEAD -- apps/web/components/PlanCard.tsx` → empty.

→ 4-layer 재정의 (D3) 및 PlanComparisonCard 본격화 (D4)는 Phase 5+ 이관 정책 유지.

---

## 5. PlanCard 무수정 검증 (사용자 결정 6-a 강제)

### 5.1 구조 검증

```tsx
// app/plan/[plan_id]/page.tsx
import PlanCard from "@/components/PlanCard";  // ★ 무수정 import

// 렌더링
plans.map((plan, i) => (
  <div key={plan.plan_id} onClick={() => handleSelect(plan.plan_id)} role="radio" ...>
    <div className="...옵션 i+1 / N 라벨...">옵션 {i + 1} / {plans.length}</div>
    <PlanCard plan={plan} />  {/* ★ props 그대로 — plan: Plan */}
  </div>
))
```

- `<PlanCard plan={plan} />` 호출 시 `plan: Plan` props 그대로 (PlanCardProps interface 변경 0건)
- selected 시각 효과는 외부 `<div>` ring 으로 처리 (PlanCard 내부 무변경)
- 옵션 번호 / 선택됨 배지는 외부 wrapper 로 추가 (PlanCard 내부 무변경)

### 5.2 git diff 검증

```bash
git diff HEAD -- harness/apps/web/components/PlanCard.tsx
# → empty (0 lines changed)
```

PASS — PlanCard.tsx 무수정 ★.

---

## 6. Phase 1 `/plan` 회귀 0 + Phase 3 `/new/*` 회귀 0

### 6.1 Phase 1 `/plan` 보존 (A4 acceptance)

`apps/web/app/plan/page.tsx` 변경:
- +14 lines: Phase 4 query `?plan_id=xxx` 감지 시 `/plan/[plan_id]` redirect (try/catch 로 graceful)
- -0 lines: Phase 1 sessionStorage envelope 흐름 / ErrorCard 표시 / PlanCard 단일 표시 모두 보존

→ Phase 1 `/plan` 진입 (sessionStorage envelope 있음) 시:
  1. useEffect 시작 → query parse → `?plan_id=` 없음 → Phase 1 흐름 진행
  2. sessionStorage `dreammate.slice6.plan` 읽음 → envelope 표시
  3. ErrorCard / Critic / RAG / PlanCard 모두 그대로 동작

→ Phase 4 query 진입 (`/plan?plan_id=xxx`) 시:
  1. useEffect 시작 → query parse → `?plan_id=xxx` 감지 → `router.replace('/plan/xxx')` → 새 route 진입

### 6.2 Phase 3 `/new/*` 회귀 0 (A5 acceptance)

- `git diff HEAD -- apps/web/app/new/` → empty
- `git diff HEAD -- apps/web/components/{discovery,common,quick}/` → empty
- `git diff HEAD -- apps/web/lib/{state/wizard,discovery_state,quick_state,mode_branching}.ts` → empty

→ Phase 3 Discovery 7-step + Quick 4-step + Mode Branching middleware 전체 무수정.

### 6.3 백엔드 회귀 0

- `git diff HEAD -- harness/backend/` → empty
- pytest 93/93 PASS (Phase 4 Slice 1 baseline 77 + Slice 2 신규 16)

---

## 7. /plan/[plan_id] page 동작 시나리오 검증 (수동 시뮬레이션)

### 7.1 Happy path (envelope 이미 생성됨)

1. 사용자가 `/plan/xxx` 진입
2. `useEffect → load()` 호출 → `getPlan('xxx')` 성공 → `resource.envelope` 존재
3. `setEnvelope(...)` → loading false
4. 3 PlanCard 세로 스택 렌더 + Critic 점수 + RAG 참조 + project_id 배지
5. 사용자가 카드 1 클릭 → `selectedPlanId === plans[0].plan_id`
6. visual ring 표시 + "선택됨" 배지 + bottom CTA "이 기획안으로 진행" 활성화
7. sessionStorage `dreammate.phase4.plan.selected.xxx = plans[0].plan_id` 저장
8. CTA 클릭 → alert (Phase 5+ 본격 흐름)

### 7.2 Cold start (envelope 미생성)

1. 사용자가 `/plan/xxx` 진입 (예: start 후 wizard 미진행 또는 generate 미호출)
2. `getPlan('xxx')` → `resource.envelope === null`
3. `generateMultiPlan('xxx')` 호출 → 30~60s 대기 (ProgressStepper 표시)
4. 성공 시 envelope 표시 / 실패 시 ErrorCard 표시

### 7.3 Error path

- 네트워크 실패 (NET-000) → ErrorCard `onRetry={load}` → 다시 시도
- INV-001 (영상기획 외 입력) → ErrorCard `onHome={()=>router.push('/')}` → 입력으로 회귀
- E-LLM-001/503 → ErrorCard `onRetry` 노출

### 7.4 sessionStorage 복원

- 사용자가 카드 선택 후 새로고침 → useEffect → sessionStorage 읽음 → `selectedPlanId` 복원 → visual ring 그대로

---

## 8. Phase 4 fetch wrapper 정합 검증

### 8.1 `generateMultiPlan(planId)` — POST `/api/v1/plans/{id}/generate`

- 성공: `{ ok: true, status: 200, envelope: MultiPlanEnvelope }`
- 실패: Phase 1 `generate()` 와 동일한 정규화 패턴
  - ErrorEnvelope (backend가 보낸 형식) → 그대로 보존
  - FastAPI default `{detail: ...}` → INV-001 / E-INV-008 합성
  - 네트워크 실패 → NET-000 합성
- `inferRetryAllowedFromCode()` 재사용

### 8.2 `getPlan(planId)` — GET `/api/v1/plans/{id}`

- 단순 throw on error (Slice 3 frontend는 catch → ErrorCard)
- 성공 시 `PlanResource` 반환 (envelope 가 null 가능)

### 8.3 `startPlan` / `wizardStep`

- Slice 3 frontend 는 직접 호출하지 않음
- Slice 4 smoke test 또는 Phase 5+ wizard 본격 구현 시 사용

---

## 9. 비용 / 응답 시간 영향

### 9.1 LLM cost

- Slice 3 frontend 자체는 LLM 호출 없음 (백엔드 endpoint 호출만)
- Cold start (`generateMultiPlan` 호출) 시 Slice 2 비용 그대로:
  - 3-plan parallel: 호출당 약 $0.001 ~ $0.003 (gpt-4o-mini × 3)
  - Critic: 호출당 약 $0.005 ~ $0.010 (gpt-4o)
  - **합계 약 $0.006 ~ $0.013 / generate 호출 1회**

### 9.2 응답 시간

- `getPlan`: 1~5 ms (in-memory store)
- `generateMultiPlan`: 30~60 sec (Slice 2 baseline)
- 동기 호출 → ProgressStepper 4-stage 시각화로 대기 UX 보강

---

## 10. 다음 액션 (Slice 4 진입 가이드)

### 10.1 Slice 4 — Final + Archive

| 산출물 | 위치 |
|---|---|
| smoke test (PowerShell, 4 endpoints + frontend route) | `scripts/smoke_test_phase_4.ps1` |
| audit_page_component.ps1 보강 (옵션) | `/plan/[plan_id]` 정규화 case 추가 (D-1 해소) |
| qa-check v1.2.0 final | `eval/qa_reports/phase-4-final_2026-05-28.md` |
| retrospective | `meta/retrospectives/phase-4.md` |
| closing_notes | `phases/active/phase-4-fastapi-extension/closing_notes.md` |
| 상태 파일 갱신 | `PROJECT_STATE.md` / `PHASE_REGISTRY.md` |
| archive 이동 | `phases/active → phases/archive/phase-4-fastapi-extension/` |

### 10.2 Slice 4 진입 전 확인

- Slice 3 commit + push 완료 (본 보고서 작성 후)
- pytest 93/93 PASS ✓
- audit_naming 0 drift ✓
- audit_page_component: 1 hit (deviations.md D-1 기록) — Slice 4에서 보강 가능
- §SELF-VERIFICATION PASS (**8연속**) ✓
- forbidden 0건 ✓
- component_map.md **14연속** 0줄 ✓
- PlanCard.tsx **3연속** 0줄 ✓
- **PlanCard.tsx 무수정 ★ (실제 사용 시점에서도 유지)**

### 10.3 다음 phase 옵션 (Slice 4 retrospective 에서 선택)

| 옵션 | 내용 | 트레이드오프 |
|---|---|---|
| **A. Phase 4.5 — Critic revise loop + Rewriter** | best plan 선택 / blocking_issues fixup / Rewriter agent | LLM cost ↑ but 품질 ↑. multi-llm-validation 권장. |
| **B. Phase 5 — Supabase Auth/DB 본격 + SSE Progress** | in-memory `_plan_store` → Supabase video_projects / wizard_states. SSE 4-stage streaming. | Auth 흐름 / RLS 정책 필요. design.md §17 본격화. |
| **C. Phase 8+ — Phase 1 endpoint 제거** | `/api/v1/generate` deprecated header 만료 후 제거 | Phase 1 회귀 모니터링 후 안전 시점. ADR-014. |

→ 사용자 결정 7 정신 반영: **Phase 4.5 vs Phase 5 우선순위**는 retrospective + multi-llm-validation 으로 결정.

### 10.4 Slice 3 ~ 4 deferred 사항 (Phase 5+ 이관 보존)

- D3 PlanCard 4-layer 재정의 → Phase 5+ (PlanComparisonCard 본격화와 함께)
- D4 PlanComparisonCard (가로 비교 + 우선순위) → Phase 5+
- D6 Critic revise loop + Rewriter → Phase 4.5
- D7 SSE Progress streaming → Phase 5+
- multi-provider (Anthropic / Google) → Phase 21+

---

## 11. 변경 이력

- 2026-05-28: Phase 4 Slice 3 최초 작성 (Frontend 3-plan minimal — `/plan/[plan_id]` PlanCard × 3 세로 스택 + 1 선택)
