# QA Check Report — Phase 3 Slice 3

> Type: slice-completion gate
> Slice: 3 / 6 (Discovery Step 2~7 + ToneChipsForm + DirectionApprovalCard verbose)
> 실행일: 2026-05-28
> 결과: **ALL PASS** (next build + tsc + lint + pytest 62/62 + audit_naming 0 drift + §SELF-VERIFICATION PASS + component_map.md 0줄 수정)
> Wave 3 병렬: Slice 4 (Quick Mode) 와 동일 worktree 공존 — 영역 격리 PASS

---

## 1. 구현 Eval (Implementation Track)

### 1.1 자동 테스트 결과

```
next build:         PASS (0 errors, ✓ Compiled successfully)
                    Route (app):
                      ○ /                          6.07 kB    93.2 kB
                      ○ /_not-found                871 B      88 kB
                      ƒ /new/discovery/step/[n]    7.42 kB    94.5 kB   ← Slice 3 신규 (dynamic)
                      ○ /new/discovery/step/1      3.16 kB    90.3 kB   (Slice 2 보존)
                      ○ /new/quick                 2.14 kB    89.3 kB   (Slice 4 영역, 동시 worktree 존재)
                      ○ /new/quick/clarify         2.21 kB    89.3 kB   (Slice 4 영역)
                      ○ /plan                      11.2 kB    98.3 kB

npx tsc --noEmit:   PASS (0 errors)

npx next lint:      PASS (✔ No ESLint warnings or errors)

pytest -q:          62 passed, 1 warning in 0.94s (Phase 1 회귀 0)
                    (실행 cwd = harness/backend/fastapi; root cwd는 package alias 문제로 skip)

audit_naming.ps1:   PASS (0 drift)
                      plan_candidates    PASS  0
                      video_projects     PASS  0
                      critic_evaluation  PASS  0
                      rag_references     PASS  0
```

### 1.2 Acceptance 매핑 (acceptance.md A3 — Discovery 확장)

| 작업 항목 | 산출 위치 | 결과 |
|---|---|---|
| `/new/discovery/step/[n]` dynamic route (n=2~7) | `apps/web/app/new/discovery/step/[n]/page.tsx` (~430 줄) | PASS |
| Step 2~4: BrandDirectionCard 패턴 재사용 | BrandCardStep sub-component (MOCK_CARDS_BY_STEP) | PASS |
| Step 5: ToneChipsForm (8 chips + skip) | `apps/web/components/discovery/ToneChipsForm.tsx` (~95 줄) | PASS |
| Step 6: DirectionApprovalCard variant=verbose | `apps/web/components/common/DirectionApprovalCard.tsx` (~165 줄) | PASS |
| Step 7: ProgressStepper (Phase 1 재사용) | Step7Generate sub-component (mock 시퀀스) | PASS |
| discovery_state.ts 확장 (Step 2~7 helpers) | `apps/web/lib/discovery_state.ts` (~108 줄 추가) | PASS |
| back navigation + state 보존 | useEffect + loadDiscoveryStep<N> | PASS |
| Step 1 → Step 2 router.push 활성 | `step/1/page.tsx` handleNext (Slice 2 TODO 해제) | PASS |
| Visual = tokens 참조 (literal 색 0건) | hex grep 결과 0 (3 신규 파일 전체) | PASS |
| next build / tsc / lint 0 errors | PASS | PASS |
| **component_map.md 0줄 수정** (조정 4번) | `git diff HEAD -- component_map.md` empty | PASS |

### 1.3 정적 검증

```
✅ 4 파일 신규 + 2 파일 수정 = 6 변경 (eval/qa_reports 1 추가 = 총 7)
   신규:
   - apps/web/components/common/DirectionApprovalCard.tsx        (NEW)
   - apps/web/components/discovery/ToneChipsForm.tsx             (NEW)
   - apps/web/app/new/discovery/step/[n]/page.tsx                (NEW, dynamic)
   - eval/qa_reports/phase-3-slice-3_2026-05-28.md               (NEW, 본 파일)
   수정:
   - apps/web/lib/discovery_state.ts                              (Step 2~7 helpers 추가, Step 1 보존)
   - apps/web/app/new/discovery/step/1/page.tsx                   (Slice 2 TODO 해제: router.push 활성)

✅ Cross-reference 정합:
   - DirectionApprovalCard props ↔ component_map.md §DirectionApprovalCard Behavior
     (direction.text / reasons / revise_count / variant / onApprove / onEditAndApprove / onRegenerate)
   - ToneChipsForm 다중선택 chip 8개 ↔ discovery_flow.md §5 (U2-7 confirmed)
   - dynamic route [n] ↔ page_map.md Routes ↔ Components 매핑표
   - discovery_state.ts Step 6 Direction ↔ direction_approval.md §4 행동 모델
   - Step 6 mock direction 생성 ↔ output_schema.md §7 P-005 oneline_direction
   - Step 7 ProgressStepper ↔ Phase 1 컴포넌트 (재사용, 보존)

✅ Phase 1 호환 보존:
   - app/page.tsx / app/plan/page.tsx 0줄 수정
   - components/{PlanCard, ErrorCard, ProgressStepper, SubmitButton}.tsx 0줄 수정
   - lib/{api, types, errors}.ts 0줄 수정
   - app/layout.tsx 0줄 수정
   - pytest 62/62 PASS (backend 회귀 0)

✅ Slice 1/2 산출물 보존:
   - apps/web/tailwind.config.ts / globals.css / lib/design_tokens.ts 0줄
   - apps/web/components/discovery/BrandDirectionCard.tsx 0줄
   - apps/web/components/discovery/CardGrid5.tsx 0줄
   - apps/web/lib/state/wizard.ts 0줄
   - apps/web/lib/discovery_state.ts Step 1 helper 본문 보존 (append만)
   - apps/web/app/new/discovery/step/1/page.tsx 수정 = Slice 2가 명시한 TODO(Slice 3) 해제만
     (handleNext 내부 router.push 활성 + Slice 2 alert 코드 제거)

✅ Read-only 영역 0줄 수정 (조정 4번 핵심):
   - apps/web/component_map.md  ← ★ 0줄 수정 (조정 4번 PASS, P-X1 third hit)
   - apps/web/page_map.md       ← 0줄 수정
   - apps/web/design_handoff.md ← 0줄 수정
   - apps/web/design_system/*   ← 0줄 수정 (4 파일)
   - apps/web/discovery_flow.md / quick_flow.md / mode_branching.md / direction_approval.md ← 0줄
   - apps/web/wireframes/*      ← 0줄 수정
   - apps/web/design.md         ← 0줄 수정
   - backend/ / docs/contracts/ / ai_system/ / knowledge/ / product/ / meta/ ← 0줄
   - .claude/skills/            ← 0줄 수정
   - PROJECT_STATE.md / PHASE_REGISTRY.md / phases/active/phase-3-pwa-impl/* (deviations 제외) ← 0줄

✅ Slice 4 영역 미침범 (★ Wave 3 병렬 race 보호):
   - apps/web/app/new/quick/*                미수정 (Slice 4 산출 — worktree에 존재)
   - apps/web/components/quick/*             미수정 (Slice 4 산출 — worktree에 존재)
   - apps/web/lib/quick_state.ts             미수정 (Slice 4 산출 — worktree에 존재)
   → Slice 4 sub-agent가 동일 worktree에서 파일 신규 작성 완료, 본 Slice 3가 0줄 수정 확인.
   → DirectionApprovalCard는 본 Slice 3에서 작성 (apps/web/components/common/), Slice 4는 import만 가능.

✅ Slice 5/6 영역 미진입:
   - apps/web/lib/mode_branching.ts          미생성 (Slice 5)
   - apps/web/app/new/page.tsx               미생성 (Slice 5)
   - apps/web/middleware.ts                  미생성 (Slice 5)
   - scripts/audit_page_component.ps1        미생성 (Slice 6)
   - scripts/smoke_test_phase_3.ps1          미생성 (Slice 6)
   - docs/decisions/phase_3_mode_branching_middleware.md (ADR-013) 미생성 (Slice 5)
```

### 1.4 qa-check v1.2.0 카테고리별

| # | 카테고리 | 결과 | 메모 |
|---|---|---|---|
| 1 | MVP 범위 | ✅ pass | non_goals 0 위반 (Phase 2 spec 0줄, Slice 4 영역 0줄, Phase 4+ 미터치) |
| 2 | API 응답 형식 | skip | mock 데이터 (Phase 4 실 API 통합 deferred) |
| 3 | 에러 상태 | skip | API 호출 없음 (Phase 4) — DirectionApprovalCard textarea 검증은 70자 cap만 |
| **4** | **모바일 화면** | ✅ pass | 360px first, max-w-2xl 중앙, sticky bottom CTA, safe-area, chip flex-wrap |
| 5 | 저장 / 재시도 | ✅ pass | sessionStorage Step 2~7 모두 + back/forward 보존 (useEffect) |
| 6 | AI 호출 정상성 | skip | mock |
| 7 | 비용 / Rate Limit | skip | mock |
| 8 | 로그 / 관측성 | skip | UI only |
| 9 | 보안 기본 | skip | UI only |
| **10** | **Simplicity Check** | ✅ pass | 5/5 (§1.5) |
| **11** | **Contract Drift (v1.2.0)** | ✅ pass | audit_naming 0 drift + pytest 62/62 |

### 1.5 Simplicity Check (qa-check v1.2.0 §10) 5/5

```
✅ 요청받지 않은 기능이 들어가지 않았는가?
   → Slice 3는 4 신규 파일 + 2 수정 파일 (Editable 목록 정합).
   → DirectionApprovalCard verbose만 4-layer chosen 구현 (minimal은 variant prop으로 분기).
   → ToneChipsForm chip 8개 + skip만 (슬라이더 X, U2-7 결정 정합).
   → Step 7 generate는 stepper mock 시퀀스만 (실 API 호출 X — Phase 4 deferred).
   → /plan 으로 redirect 시 Phase 1 entry point만 도착 (envelope 전달 X — Phase 4).

✅ 단일 사용 추상화가 생기지 않았는가?
   → DirectionApprovalCard: Discovery Step 6 + Quick Mode 양쪽 사용 (variant 분기) → 재사용성 명확.
   → BrandCardStep sub-component: n=2/3/4 세 step 공유 → 명확.
   → saveDiscoveryStep<N> generic: Step 1~7 모두 type-safe 저장 → 다중 사용.
   → buildMockDirection: Step 6 진입 + 재생성 두 호출 지점 → 다중 사용.

✅ Slice 3에서 Slice 5+ 기능을 미리 구현하지 않았는가?
   → mode_branching 로직 X (Slice 5).
   → /new/page.tsx (mode 진입 redirect) X (Slice 5).
   → audit_page_component / smoke_test_phase_3 X (Slice 6).
   → Quick Mode 4-step routes X (Slice 4 영역 — 영역 격리).

✅ 500줄짜리 구현이 200줄로 줄어들 수 있지 않은가?
   → DirectionApprovalCard.tsx ~165 줄: verbose layout + editMode 토글 + 70자 카운터 + 3 버튼 + a11y
   → ToneChipsForm.tsx ~95 줄: chip 8개 + maxSelections 카운터 + skip + a11y (role=group + checkbox)
   → [n]/page.tsx ~430 줄: 7 step 분기 (4 sub-component) + mock 데이터 3 step × 4 cards + WizardHeader 공통
     - 분리 가능성 검토: Step 2~4를 별도 파일로 쪼개도 mock 데이터가 결국 어디론가 가야 함.
       dynamic route 단일 파일 + sub-component 패턴이 가장 응집도 높음.
   → discovery_state.ts +108 줄: Step 2~7 interface 6개 + generic save/load → type-safe 위해 필요한 최소량.

✅ 기존 문서/코드의 unrelated formatting을 바꾸지 않았는가?
   → component_map.md / page_map.md / design_handoff.md / design_system/* 0줄
   → BrandDirectionCard.tsx / CardGrid5.tsx (Slice 2) 0줄
   → wizard.ts / design_tokens.ts (Slice 1/2) 0줄
   → Phase 1 컴포넌트 0줄
   → step/1/page.tsx 수정은 본문 변경 X, TODO 주석 행 + alert 블록만 제거 (Slice 2 명시한 자리).
```

### 1.6 Contract Drift Check (qa-check v1.2.0 §11)

```bash
$ powershell -ExecutionPolicy Bypass -NoProfile -File scripts/audit_naming.ps1

plan_candidates    PASS  0
video_projects     PASS  0
critic_evaluation  PASS  0
rag_references     PASS  0

audit_naming PASS — 0 drift detected
```

---

## 2. 산출물

### 2.1 신규 파일 (4개 코드/페이지 + 1 QA report = 5)

```
apps/web/components/common/
  DirectionApprovalCard.tsx                       (~165 줄, 4-layer chosen=verbose + minimal variant prop)

apps/web/components/discovery/
  ToneChipsForm.tsx                               (~95 줄, 8 chips + maxSelections=4 + skip + a11y)

apps/web/app/new/discovery/step/[n]/
  page.tsx                                        (~430 줄, dynamic route n=2~7 dispatcher)

eval/qa_reports/
  phase-3-slice-3_2026-05-28.md                   (본 파일)
```

### 2.2 수정 파일 (2개)

```
apps/web/lib/discovery_state.ts                    (Step 1 helper 보존 + Step 2~7 interface 6 + generic save/load)
apps/web/app/new/discovery/step/1/page.tsx         (handleNext 내부 router.push 활성 — Slice 2 TODO 해제)
```

### 2.3 단위 테스트 산출물

Slice 2와 동일 — jest/react-testing-library 미설치 → **선택 산출물 skip**.
대안: next build 통과 + tsc strict + ESLint clean + dynamic prerender 성공 (`ƒ /new/discovery/step/[n]`)로 smoke level 1 통과.

### 2.4 통계

| 항목 | 값 |
|---|---|
| 신규 파일 | 4 (코드 3 + QA report 1) |
| 수정 파일 | 2 (discovery_state.ts + step/1/page.tsx) |
| 신규 줄 수 | ~690 (코드) + QA report |
| 수정 줄 수 | +105 / −14 |
| pytest 회귀 | 62/62 PASS (변경 0) |
| audit_naming | 0 drift |
| next build | 0 errors |
| tsc | 0 errors |
| ESLint | 0 warnings/errors |
| literal hex (3 신규 파일) | 0 |
| component_map.md 수정 | 0줄 ★ (조정 4번 PASS, third consecutive) |

### 2.5 검증 grep 결과

```bash
# Phase 3 Slice 3 신규 코드 영역 — literal hex 검증
$ grep -rE "#[0-9A-Fa-f]{3,8}\b" apps/web/components/common/
# 결과: 0 (PASS)

$ grep -rE "#[0-9A-Fa-f]{3,8}\b" apps/web/components/discovery/ToneChipsForm.tsx
# 결과: 0 (PASS)

$ grep -rE "#[0-9A-Fa-f]{3,8}\b" apps/web/app/new/discovery/step/[n]/
# 결과: 0 (PASS)

# Visual layer 모두 Tailwind utility (bg-primary / bg-bg-subtle / text-text-default /
# border-border-default / border-state-error / text-text-danger 등) → var(--color-*) alias.
# replaceability L 보장 (시나리오 1 token 1줄 변경 자동 반영).
```

---

## 3. 구현 세부 (Slice 2 baseline 재사용)

### 3.1 DirectionApprovalCard (양 모드 공통)

```
variant=verbose (Discovery Step 6 chosen):
  - 한 줄 방향 카드 (bg-bg-subtle, radius-lg, ✎ icon)
  - 구분선 + reasons[] list (Step 1~5 요약)
  - 버튼 3개: "이대로 진행 ▶" (primary) + "다시 생성 ↻" (tertiary)
    + (편집 모드 진입 시) primary 라벨 "수정 후 진행 ▶" 자동 변경

variant=minimal (Quick Mode, Slice 4 import용):
  - 한 줄 방향 카드 동일
  - reasons 영역 자동 hidden (showReasons 가드)
  - 버튼 2개 (구조 동일)
```

편집 모드:
- ✎ 클릭 → textarea 진입 + Esc로 취소
- 글자수 카운터 (≤ 70자 — output_schema §7.2 정합)
- 70자 초과 시 text-danger + primary disabled (aria-invalid="true")
- direction.text prop 변경 (재생성) 시 useEffect로 buffer 동기화 + editMode 자동 해제

a11y:
- role="region" + 명시 aria-label="기획 방향 승인"
- textarea role 자동 (HTML5) + aria-multiline + aria-label + aria-invalid
- 모든 버튼 aria-label 명시
- aria-live="polite" (글자수 카운터)

### 3.2 ToneChipsForm (Step 5 form 변형)

```
8 chips (TONE_CHIPS const):
  warm / professional / casual / energetic / sincere / humorous / informative / friendly

maxSelections=4 (default — discovery_flow.md §5.1 1~4 범위 chosen)
선택된 chip: bg-primary + text-text-inverse + border-primary
미선택 chip: bg-surface + text-text-default + border-border-default
maxSelections 도달 시 미선택 chip: disabled (bg-bg-subtle + cursor-not-allowed)
skip: 별도 underline 버튼 → onSkip 호출 → 부모가 router.push

a11y:
- role="group" + 명시 aria-label
- 각 chip role="checkbox" + aria-checked + 명시 aria-label
- aria-live="polite" (선택 개수 카운터)
- Tab 진입 → 8 chip 순환 + Enter/Space로 toggle
```

### 3.3 Dynamic route /new/discovery/step/[n]

```
useParams() → n = Number(params.n)
  ↓ n ∈ {2,3,4} → <BrandCardStep n={n} />
                  - MOCK_CARDS_BY_STEP[n] + USER_INPUT_SLOT_BY_STEP[n]
                  - CardGrid5 (Slice 2 재사용) + sticky CTA
                  - saveDiscoveryStep(n, state) → router.push(`/new/discovery/step/${n+1}`)
  ↓ n === 5     → <Step5Tone />
                  - ToneChipsForm + maxSelections=4 + skip
                  - saveDiscoveryStep(5, {selectedTones, skipped})
  ↓ n === 6     → <Step6Direction />
                  - buildMockDirection: sessionStorage Step 1~5 → reasons 합성
                  - DirectionApprovalCard variant="verbose"
                  - onApprove / onEditAndApprove / onRegenerate (revise_count++)
  ↓ n === 7     → <Step7Generate />
                  - ProgressStepper currentStep 시퀀스 (intent → rag → planning → critic → complete)
                  - mock 800ms tick × 5 (Phase 4 실 API 통합 시 SSE 교체)
                  - complete 시 "기획안 보기" → /plan (Phase 1 entry)
  ↓ 그 외       → notFound()
```

### 3.4 discovery_state.ts 확장

```typescript
// Slice 2 보존: DiscoveryStep1State + saveDiscoveryStep1 + loadDiscoveryStep1
// Slice 3 추가:
//   - DiscoveryStep2State / 3 / 4 (BrandDirectionCard 패턴, Step 1과 동일 형식)
//   - DiscoveryStep5State (selectedTones[], skipped)
//   - DiscoveryStep6State (direction + approved + editedText)
//   - DiscoveryStep7State (generatedAt + envelope)
//   - saveDiscoveryStep<N>(n, state) / loadDiscoveryStep<N>(n) — type-safe generic
```

---

## 4. P-X1 §SELF-VERIFICATION 결과 ★ Phase 3 third hit

> phase-start v1.3.0 §6.3 — Sub-agent 자기 검증 (P-AGENT-SCOPE-001 대응)

### 4.1 git status + git diff --stat HEAD 실행

```bash
$ git status
On branch main
Your branch is up to date with 'origin/main'.

Changes not staged for commit:
  modified:   harness/apps/web/app/new/discovery/step/1/page.tsx
  modified:   harness/apps/web/lib/discovery_state.ts

Untracked files:
  harness/apps/web/app/new/discovery/step/[n]/
  harness/apps/web/app/new/quick/                          ← Slice 4 sub-agent 산출 (worktree 공존)
  harness/apps/web/components/common/
  harness/apps/web/components/discovery/ToneChipsForm.tsx
  harness/apps/web/components/quick/                        ← Slice 4 sub-agent 산출 (worktree 공존)
  harness/apps/web/lib/quick_state.ts                       ← Slice 4 sub-agent 산출 (worktree 공존)
  "사진/"                                                    (pre-existing, 무관)

$ git diff --stat HEAD
 harness/apps/web/app/new/discovery/step/1/page.tsx |  11 +--
 harness/apps/web/lib/discovery_state.ts            | 108 ++++++++++++++++++++-
 2 files changed, 105 insertions(+), 14 deletions(-)
```

### 4.2 editable / forbidden 비교

| 파일 | 영역 | 상태 | 판정 |
|---|---|---|---|
| `apps/web/components/common/DirectionApprovalCard.tsx` | editable (NEW) | untracked | ✅ |
| `apps/web/components/discovery/ToneChipsForm.tsx` | editable (NEW) | untracked | ✅ |
| `apps/web/app/new/discovery/step/[n]/page.tsx` | editable (NEW) | untracked | ✅ |
| `apps/web/lib/discovery_state.ts` | editable (UPDATE) | modified | ✅ |
| `apps/web/app/new/discovery/step/1/page.tsx` | editable (Slice 2 TODO 해제만) | modified | ✅ |
| `eval/qa_reports/phase-3-slice-3_2026-05-28.md` | editable (NEW) | untracked | ✅ |
| `apps/web/component_map.md` ★ | forbidden (조정 4번) | unchanged | ✅ PASS |
| `apps/web/page_map.md` / `design_handoff.md` / `design.md` | forbidden | unchanged | ✅ |
| `apps/web/design_system/*` (4 파일) | forbidden | unchanged | ✅ |
| `apps/web/*flow.md` / `direction_approval.md` | forbidden | unchanged | ✅ |
| `apps/web/wireframes/*` | forbidden | unchanged | ✅ |
| Slice 1/2 산출물 (tailwind.config / globals.css / design_tokens / wizard.ts / BrandDirectionCard / CardGrid5) | 보존 | unchanged | ✅ |
| Slice 4 영역 (`app/new/quick/*` / `components/quick/*` / `lib/quick_state.ts`) ★ | forbidden 본 Slice 입장 | untracked (Slice 4 own) | ✅ 미수정 |
| Slice 5/6 영역 (mode_branching / `/new/page.tsx` / scripts/*) | forbidden | unchanged | ✅ |
| Phase 1 components / lib / app pages | 보존 | unchanged | ✅ |
| `backend/`, `docs/contracts/`, `ai_system/`, `knowledge/`, `product/`, `meta/`, `.claude/skills/` | forbidden | unchanged | ✅ |
| `PROJECT_STATE.md` / `PHASE_REGISTRY.md` / `phases/active/phase-3-pwa-impl/*` (deviations 제외) | forbidden | unchanged | ✅ |

### 4.3 판정

**PASS** — modified 2 (둘 다 editable) + untracked 4 신규 파일 (모두 editable). Slice 4 영역 untracked 파일 3개는 본 Slice 3가 0줄 수정 (병렬 작업 영역 격리 보장).

**조정 4번 핵심**: `apps/web/component_map.md` **0줄 수정 확인** ★ (P-AGENT-SCOPE-001 대응 third consecutive hit — Slice 1+2+3 모두 PASS).

**Wave 3 병렬 보호**: Slice 4 영역 (`app/new/quick/*` / `components/quick/*` / `lib/quick_state.ts`) 본 Slice가 0줄 수정 — `git diff HEAD` 결과 본 영역 항목 0건 (modified 항목은 본 Slice 3 editable 2 파일만).

→ P-X1 §SELF-VERIFICATION third hit 성공. Phase 3 Slice 1+2+3 = 3연속 PASS.

---

## 5. component_map.md 0줄 수정 확인 (조정 4번)

```bash
$ git diff HEAD -- harness/apps/web/component_map.md
# (empty)

$ git diff HEAD -- harness/apps/web/page_map.md harness/apps/web/design_handoff.md harness/apps/web/design.md
# (empty)

$ git diff HEAD -- harness/apps/web/design_system/ harness/apps/web/wireframes/
# (empty)

$ git diff HEAD -- harness/apps/web/discovery_flow.md harness/apps/web/quick_flow.md harness/apps/web/mode_branching.md harness/apps/web/direction_approval.md
# (empty)
```

→ Phase 2 spec **전체 0줄 수정** 확인. Slice 3 산출 4 파일 + 2 수정 모두 Phase 2 spec contract를 1:1 그대로 구현 (DirectionApprovalCard props ↔ component_map.md §DirectionApprovalCard Behavior, ToneChipsForm 8 chips ↔ discovery_flow.md §5.1 U2-7, dynamic route [n] ↔ page_map.md Routes 매핑).

→ deviation 0건. Phase 2 spec 변경 없이 코드로 컴파일 — 변경성 시뮬레이션 #2 (variants swap) 보존.

---

## 6. Wave 3 병렬 race 처리

### 6.1 동시 작업 영역

```
Slice 3 (본 작업):
  - apps/web/components/common/DirectionApprovalCard.tsx     [본 Slice가 작성]
  - apps/web/components/discovery/ToneChipsForm.tsx
  - apps/web/app/new/discovery/step/[n]/page.tsx
  - apps/web/lib/discovery_state.ts (확장)
  - apps/web/app/new/discovery/step/1/page.tsx (TODO 해제)

Slice 4 (병렬 sub-agent):
  - apps/web/components/quick/QuickInputCard.tsx
  - apps/web/app/new/quick/page.tsx
  - apps/web/app/new/quick/clarify/page.tsx
  - apps/web/app/new/quick/direction/page.tsx       [DirectionApprovalCard import]
  - apps/web/app/new/quick/generate/page.tsx
  - apps/web/lib/quick_state.ts
```

### 6.2 race 발생 여부

```
worktree 검사:
  $ git status
  → Slice 4 산출 파일이 untracked로 본 worktree에 이미 존재
  → Slice 4 sub-agent가 본 작업과 동일 filesystem에서 파일 생성 완료

본 Slice 3 침범 검사:
  $ git diff HEAD -- apps/web/app/new/quick/ apps/web/components/quick/ apps/web/lib/quick_state.ts
  → 결과: 0 (본 Slice 3는 Slice 4 영역 0줄 수정)

Slice 4의 본 Slice 3 영역 검사:
  $ git status | grep "components/common"
  → untracked: components/common/ (본 Slice 3가 신규 작성한 DirectionApprovalCard)
  → Slice 4가 본 영역을 미터치 (DirectionApprovalCard는 본 Slice 3 산출, Slice 4는 import만)

next build:
  → /new/quick + /new/quick/clarify 라우트가 함께 컴파일됨 (Slice 4 산출이 worktree 공존)
  → 본 Slice 3 산출 ƒ /new/discovery/step/[n] 정상 dynamic prerender
  → 충돌 0건 (모든 import 정합)
```

### 6.3 push 시 race 처리 계획

```
origin/main HEAD = a174a72 (Slice 2)

본 Slice 3 commit 생성 후:
  $ git push origin main
  → 첫 push 시 Slice 4가 이미 push했다면 reject (fast-forward 불가)
  → 대응: git pull --rebase origin main → git push origin main

본 Slice 3 commit 범위:
  - 본 Slice 3 산출 4 신규 + 2 수정 + QA report만 stage
  - Slice 4 untracked 파일은 stage X (Slice 4 own commit)
  → "git add" 시 명시 path 사용 (git add -A 절대 X)
```

---

## 7. 다음 액션 (Slice 5 진입 가이드)

### 7.1 Slice 5 작업 시작 권한

```
✅ Slice 5 (Mode Branching middleware) 진입 가능 조건:
   - Slice 3 완료 (본 Slice) — A3 PASS
   - Slice 4 완료 (병렬 작업, 별도 commit) — A4 PASS 검증 필요

→ work_plan.md Slice 5 산출물:
   - apps/web/lib/mode_branching.ts (4 branching_rules + 3 override_rules)
   - apps/web/app/new/page.tsx (진입점, redirect)
   - (선택) apps/web/middleware.ts (ADR-013 결정)
   - docs/decisions/phase_3_mode_branching_middleware.md (ADR-013)
   - eval/qa_reports/phase-3-slice-5_2026-05-28.md
```

### 7.2 Slice 3 미해결 / Open Items

- **Phase 4 실 API 통합 deferred**:
  - Step 2~4 mock 데이터 → P-002/003/004 endpoint fetch (output_schema.md §4/§5/§6)
  - Step 6 buildMockDirection → P-005 oneline_direction endpoint
  - Step 7 ProgressStepper mock 800ms tick → 실 SSE (Phase 4) 또는 polling
- **DirectionApprovalCard 최대 글자 검증 강화**: 현재 70자 cap만, < 20자 검증 (output_schema §7.2)은 Phase 4에서 적용
- **WizardHeader 컴포넌트 추출**: 현재 [n]/page.tsx 내부 inline → Slice 5/6에서 별도 파일 검토
- **단위 테스트**: jest/react-testing-library 미설치 → Phase 4+ test infra 도입 후
- **revise_count cap 3회 mode-down 제안 UI**: direction_approval.md §11 Phase 3 deferred 명시 — Phase 4

→ Slice 5 진행 차단 요소 없음.

### 7.3 Phase 3 unresolved uncertainties 상태 (assumptions.md §1.2)

| ID | 상태 (Slice 3 종료) | 비고 |
|---|---|---|
| U3-1 (spec ↔ 코드 drift) | ✅ Slice 1+2+3 = 3연속 0건 (Phase 2 spec 0줄) | Slice 6 변경성 시뮬 |
| U3-2 (sub-agent forbidden 침범 — P-X1 효과) | ✅ Slice 1+2+3 = 3연속 PASS (§4) | 매 Wave 검증 |
| U3-3 (hardcoded 색 검출) | ✅ Slice 1+2+3 = 3연속 0건 | Slice 4~ 회귀 |
| U3-4 (Phase 1 endpoint vs Discovery 어댑터 비용) | 미검증 (mock 데이터만, Phase 4 deferred) | |
| U3-5 (Step 5 chip 개수) | ✅ 8 chips chosen (`TONE_CHIPS` const), maxSelections=4 default | Phase 4+ 실 데이터로 조정 |
| U3-6 (session storage 충돌) | ✅ wizard.ts prefix 분리 + Step 1~7 모두 PASS | Slice 4 Quick state 합류 후 추가 검증 |
| U3-7 (component_map.md 직접 수정 충동) | ✅ Slice 1+2+3 = 3연속 0건 (조정 4번 PASS) | 매 Slice deviation 검증 |
| U3-8 (Wave 3 병렬 race) | ✅ Slice 3 commit 준비 시점 race 0건 (Slice 4 산출이 worktree 공존, 본 Slice 0줄 수정) | push 시 git pull --rebase 대응 계획 |

---

## 8. 변경 이력

- 2026-05-28: Phase 3 Slice 3 완료, qa-check v1.2.0 카테고리 1/4/5/10/11 PASS + Simplicity 5/5 + audit_naming 0 drift + pytest 62/62 + next build 0 + tsc 0 + lint 0 + §SELF-VERIFICATION PASS (P-X1 third consecutive hit) + component_map.md 0줄 수정 (조정 4번 third consecutive PASS) + Wave 3 병렬 Slice 4 영역 0줄 수정 (race 보호)
