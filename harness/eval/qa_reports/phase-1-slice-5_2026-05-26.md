# QA Check Report — Phase 1 Slice 5

> Type: slice-completion gate
> Slice: 5 / 7 (Supabase persistence + graceful failure)
> 실행일: 2026-05-26
> 결과: **ALL PASS** (62/62 pytest + 정적 검증 통과, Phase 1 종료 진입 권한 부여)

---

## 1. 구현 Eval (Implementation Track)

### 1.1 자동 테스트 결과

```bash
$ python -m pytest -v
============================= test session starts =============================
collected 62 items

backend/fastapi/tests/test_critic.py::               13 PASSED
backend/fastapi/tests/test_db.py::                    9 PASSED   ← NEW (Slice 5)
backend/fastapi/tests/test_e2e_slice1.py::           19 PASSED   (15 + 4 신규)
backend/fastapi/tests/test_intent.py::                8 PASSED
backend/fastapi/tests/test_planning.py::              5 PASSED
backend/fastapi/tests/test_rag_fallback.py::          8 PASSED
======================== 62 passed, 1 warning in 0.93s ========================
```

- 신규 테스트: 13개 (`test_db.py` 9 + `test_e2e_slice1.py` 4 추가)
- 누적 회귀 통과: 49/49 (Slice 1/2/3/4 영역) → 62/62 (Slice 5 포함)
- 회귀 통과율: 100% (기존 어떤 테스트도 깨지지 않음 — backward compat 유지)

### 1.2 Acceptance 매핑 (work_plan.md Slice 5)

| Acceptance | 테스트 / 산출물 | 결과 |
|---|---|---|
| `video_projects` row 생성 확인 | `test_save_success_returns_ids` (project_id 반환) | ✅ pass |
| `plan_candidates` row 생성 확인 | `test_save_success_returns_ids` (plan_candidate_ids 반환) + `test_raw_llm_json_includes_plan_critic_and_rag` | ✅ pass |
| DB 연결 실패 시도 응답 정상 (`meta.project_id == null`) | `test_generate_db_failure_still_returns_200` (HTTP 200 + project_id null) + `test_generate_db_skipped_still_returns_200` | ✅ pass |
| `pytest backend/tests/db.py` 통과 | `test_db.py` 9/9 | ✅ pass |
| 인증 없이 익명 저장 (`user_id` NULL 허용) | `repositories/video_project.py::insert_video_project` row dict 에 `user_id: None` 고정 + migration SQL `user_id UUID NULL` | ✅ pass |
| 저장 실패 시 응답은 정상 (저장 오류로 사용자 차단 금지) | `test_router_returns_200_even_when_db_fails` (HTTP 200 보장) | ✅ pass |
| 저장 결과를 `meta.project_id`에 포함 | `test_generate_includes_project_id` (mock_pipeline_ok 통과 시 fake-project-uuid 노출) | ✅ pass |

### 1.3 정적 검증

```
✅ py_compile: 신규/변경 14 파일 syntax OK
✅ import path: backend.fastapi.db.* 정상 해석
   - db/__init__.py exports: save_video_planning, get_supabase_client, PersistenceResult, SaveStatus
   - db.repositories: insert_video_project, insert_plan_candidate
   - db.types: PersistenceResult / SaveStatus
✅ 순환 import 없음:
     db/types.py            — Pydantic만 import (외부 의존 없음)
     db/supabase_client.py  — config 만 import (graceful import — supabase 미설치도 OK)
     db/repositories/*.py   — 외부 의존 없음 (graceful try/except)
     db/__init__.py         — repositories + supabase_client + types 통합
     schemas/output.py      — Meta.project_id 추가 (Optional, 기본값 None)
     routers/generate.py    — db.save_video_planning import (단방향)
✅ Pydantic 모델: PersistenceResult (status + project_id + plan_candidate_ids + error_reason)
✅ Pydantic 모델: Meta.project_id: str | None = Field(default=None) — backward compat
✅ FastAPI 응답 모델: meta.project_id 노출 (test_generate_includes_project_id 통과)
✅ OpenAPI 문서 (/openapi.json): meta.project_id Optional 노출 + validation.checks.db_persistence 노출
✅ supabase>=2.7 requirements.txt 추가 (install 미설치도 import-time graceful skip)
✅ backend.fastapi.__version__ == "0.5.0", app.version == "0.5.0", /health slice="5"
```

### 1.4 qa-check v1.1.0 카테고리별 (Slice 5 적용 항목)

| # | 카테고리 | 결과 | 메모 |
|---|---|---|---|
| 1 | MVP 범위 | ✅ pass | DB 저장 단건 (video_projects + plan_candidates 각 1 row). Auth/RLS/series/brand/domain 모두 미포함 (Phase 5+/Phase 2+). |
| 2 | API 응답 형식 | ✅ pass | meta.project_id 추가 (Optional → backward compat 유지). validation.checks에 db_persistence 추가. envelope 구조 불변. |
| 3 | 에러 상태 | ✅ pass | **DB 실패는 절대 사용자 차단 안 함**. 3가지 status (`saved` / `skipped_no_db` / `failed_db_error`) 차별 기록 + validation.checks.db_persistence 로 관측. |
| 4 | 모바일 화면 | skip | Slice 6+ (apps/web/ 영역 — 별도 sub-agent 작업 중) |
| 5 | 저장 / 재시도 | ✅ pass | Slice 5의 핵심. 저장 성공 시 project_id 노출, 실패/skip 시 None. 재시도는 Phase 2+ exponential backoff (현 Phase는 동기 단일 시도). |
| 6 | AI 호출 정상성 | ✅ pass | Intent(P-001) → RAG(retriever) → Planning(P-006 + rag_context) → Critic(P-007) → DB save (graceful) 직렬. |
| 7 | 비용 / Rate Limit | partial | DB insert 호출 1~2회 (Supabase 무료티어 충분). LLM 호출 비용은 Slice 4 동일. agent_io_logs는 Phase 2+ 도입. |
| 8 | 로그 / 관측성 | ✅ pass | `logger.info("generate ok ... db_status=%s project_id=%s")` + repositories 내부 실패 시 warning. validation.checks.db_persistence 응답 노출. |
| 9 | 보안 기본 | ✅ pass | SUPABASE_URL/ANON_KEY는 .env (커밋 차단). 익명 저장이므로 service_role 사용 안 함. RLS 정책은 Phase 5 Auth 도입 시점 정합. |
| **10** | **Simplicity Check** | ✅ pass | 5/5 (아래 §1.5) |

### 1.5 Simplicity Check (v1.1.0 §10)

```
✅ 요청받지 않은 기능이 들어가지 않았는가?
   → Slice 5는 video_projects + plan_candidates 단건 insert만. 
     Retry / queue / agent_io_logs / RLS / series/brand FK / migration runner / dual-write
     모두 미포함.
✅ 단일 사용 추상화가 생기지 않았는가?
   → save_video_planning 1개 orchestrator 함수 + 2개 repository 함수 + 1개 client wrapper.
     클래스 0개. PersistenceResult 1개 Pydantic 모델만.
✅ Phase 1에서 Phase 2~5 기능을 미리 구현하지 않았는가?
   → Auth (user_id NOT NULL + RLS), agent_io_logs 테이블, plan_options vs plan_candidates 
     contract 정합, brand/domain/series FK, soft delete, audit log — 모두 미포함.
     migration SQL 도 Phase 5 진입 시 ALTER 권고만 주석으로 남김.
✅ 200줄짜리 구현이 50줄로 줄어들 수 있지 않은가?
   → save_video_planning 80줄 (3 단계 분기 + 부분 성공 추적 + 로깅 포함).
     repositories/video_project.py 60줄 / plan_candidate.py 70줄 (graceful try/except 필수).
     compression 한계 — `failed_db_error` 부분 성공 vs 전체 실패 분기는 acceptance 요구사항.
✅ 기존 문서/코드의 unrelated formatting을 바꾸지 않았는가?
   → contracts / knowledge / ai_system / apps/web 0줄 수정.
     Slice 2/3/4 안정화된 intent.py / critic.py / rag/* / planning.py 0줄 수정.
     Meta.make 시그니처는 backward compat 유지 (request_id / project_id 모두 keyword + Optional).
```

---

## 2. 플랫폼 품질 Eval (Product Quality Track)

### 2.1 Slice 5 비중

`eval/INDEX.md` §4 Phase 1 비중표 기준: Slice 5는 **DB 실패 시 사용자 차단 0건 보장**이 핵심
품질 게이트. work_plan.md §"Slice별 Eval 매핑" 에 명시된 별도 품질 평가 항목 없음 (e2e 응답 정상성).

### 2.2 Graceful Persistence 검증 시나리오 (사용자 차단 0건)

본 Slice의 **최우선 acceptance**: 어떤 DB 실패 경로에서도 사용자 요청을 차단하지 않는다.

| 실패 시나리오 | 검증 테스트 | save_status | router 응답 |
|---|---|---|---|
| Supabase env 미설정 (SUPABASE_URL/ANON_KEY 없음) | `test_get_client_returns_none_when_env_missing` + `test_generate_db_skipped_still_returns_200` | `skipped_no_db` | 200 OK + meta.project_id=null |
| supabase-py 패키지 미설치 (ImportError) | (supabase_client.py:35 의 try/except ImportError → None) | `skipped_no_db` | 200 OK + meta.project_id=null |
| create_client 자체가 예외 (잘못된 URL 등) | (supabase_client.py:41 의 try/except → None + warning) | `skipped_no_db` | 200 OK + meta.project_id=null |
| video_projects insert 예외 (DB 연결 끊김 / SQL 오류) | `test_save_handles_video_projects_insert_exception` + `test_generate_db_failure_still_returns_200` | `failed_db_error` (reason=video_projects_insert_failed) | 200 OK + meta.project_id=null |
| video_projects insert 응답 data 비어있음 (Supabase 권한 거절 등 silent) | `test_save_handles_empty_insert_response` | `failed_db_error` | 200 OK + meta.project_id=null |
| video_projects 성공 + plan_candidates insert 예외 (부분 실패) | `test_save_handles_plan_candidates_insert_exception` | `failed_db_error` (reason=plan_candidates_insert_failed, project_id 채워짐) | 200 OK + meta.project_id=video_projects.id (부분 성공 추적) |
| save_video_planning 자체가 예상치 못한 예외 raise | (router 방어 try/except → PersistenceResult(failed_db_error, unexpected_exception)) | `failed_db_error` | 200 OK + meta.project_id=null |

**핵심 보장**: 어떤 경로에서도 HTTP 500/502 발생 안 함. DB 실패는 항상 validation.checks의
`db_persistence status="warn"` + detail 필드로 관측 가능하되 사용자 응답은 정상 200.

### 2.3 graceful policy — RAG (Slice 4) 와 동일 패턴 정합

```
Slice 4 (RAG)              | Slice 5 (DB)
─────────────────────────────|─────────────────────────────────
RetrievalResult              | PersistenceResult
references / used_fallback   | project_id / status
fallback_reason (4 종)        | error_reason (자유 문자열)
validation.checks.rag_retrieval | validation.checks.db_persistence
status: ok | warn             | status: ok | warn
사용자 차단 0건 (HTTP 200 보장) | 사용자 차단 0건 (HTTP 200 보장)
```

→ Phase 1 "graceful sub-system" 컨벤션이 RAG와 DB 두 모듈에 일관 적용됨.
Phase 2+에서 추가 sub-system (Email / Webhook 등)도 동일 패턴 채용 권장.

### 2.4 raw_llm_json 통합 저장 검증

Slice 4 QA report §5.3(a) 인계 가이드 적용:

```json
plan_candidates.raw_llm_json = {
    "plan": { ...Plan.model_dump() },
    "critic_evaluation": { ...CriticEvaluation.model_dump() },
    "rag_references": [ { source_id, title, snippet, similarity, ... }, ... ]
}
```

검증 테스트: `test_raw_llm_json_includes_plan_critic_and_rag` —
실제 insert row 의 raw_llm_json 에 3 가지 키 모두 존재 + RAG references 가 dict 배열로 보존됨.

Phase 2+ 정규화 옵션 (별도 `plan_candidate_rag_usage` 테이블) 은 contract-change Skill 절차로
추후 도입 권장 (rag_data_contract §5.5 참조). Phase 1 단계에서 JSONB 통합 저장으로 충분.

### 2.5 라이브 Supabase 호출 미수행 사유

Phase 1 Slice 5는 mock 검증만 수행 (Slice 4와 동일 정책). 실 운영 검증은:
- 사용자가 `.env` 에 `SUPABASE_URL` + `SUPABASE_ANON_KEY` 입력
- Supabase Dashboard SQL Editor 에서 `db/migrations/001_init.sql` 실행
  (또는 `supabase db push` migration 명령)
- backend 재기동 후 자동 retriever + DB save 분기 진입.

migration 적용은 manual 절차로 가이드 (work_plan.md §"Phase 1 종료 직전 smoke test" 에서 1회 검증).

---

## 3. Slice 4 deviation 흡수 / 컨벤션 유지

Slice 4 QA report (`phase-1-slice-4_2026-05-26.md` §5.3) 인계 가이드 반영:

| 인계 항목 | Slice 4 결정 | Slice 5 반영 |
|---|---|---|
| (a) rag_references → plan_candidates 저장 | `raw_llm_json` JSONB 통합 권장 | ✅ `plan_candidate.py::insert_plan_candidate` 의 `raw_llm_json = {"plan", "critic_evaluation", "rag_references"}` 통합 저장 |
| (b) DATABASE_URL vs SUPABASE_URL 분리/통합 | 권장: 통합 운영 (Phase 1) | ✅ supabase_client.py 는 `SUPABASE_URL` + `SUPABASE_ANON_KEY` 만 사용 (pgvector는 별도 `PGVECTOR_DATABASE_URL` 유지). 통합 운영은 사용자가 .env에 동일 Postgres 지정하면 자동 정합. |
| (c) RAG fallback 추적 (agent_io_logs 컬럼) | Phase 2+ 도입 권장 | ⏭️ 본 Slice 미도입 (Phase 2 Skill). 현재는 router logger 만 사용. |
| (d) router 시그니처 안정성 | `rag_result.references` + `fallback_reason` 모두 router 로컬 변수 | ✅ 유지 — Slice 5에서 rag_refs_serialized 만 추가 (rag_refs.model_dump 변환). |

Slice 1~3 안정화 컨벤션 유지:

| 컨벤션 | 결정 | Slice 5 유지 여부 |
|---|---|---|
| Critic 8 차원 0~5 정수 점수 | 8 차원 (output_schema §9.1) | ✅ 유지 (critic.py 0줄 수정) |
| `meta.prompt_id` = 응답 본문 생성기 (Planning) | `P-006` | ✅ 유지 |
| `validation.checks` 다단 노출 | 5개 항목 (Slice 4까지) | ✅ 유지 + **db_persistence 6번째 항목 추가** |
| INV-001 ErrorEnvelope | 활성화 | ✅ 유지 |
| graceful sub-system 패턴 | Slice 4 (RAG) 도입 | ✅ Slice 5 (DB) 가 동일 패턴 채용 (PersistenceResult + validation.checks + 사용자 차단 0건) |

추가된 Slice 5 컨벤션:
- `meta.project_id: str | None` — Optional 필드 (backward compat). DB 미사용 / 실패 시 None.
- `Meta.make()` 시그니처 — `request_id` + `project_id` keyword 인자 추가 (Optional, backward compat).
  → DB save 시 video_projects.request_id 와 envelope.meta.request_id 가 동일 uuid (관측성).
- `validation.checks` 6번째 항목: `db_persistence, status=ok|warn, detail="status=..., project_id=..., reason=..."`
- `validation.warnings` 변동 없음. 잔존 경고: `phase_1_single_plan` (Phase 4+ 해소 예정).

---

## 4. 산출물 / 코드 통계

### 4.1 신규 파일 (10개)

```
backend/fastapi/db/__init__.py                            — 모듈 exports + save_video_planning orchestrator (110줄)
backend/fastapi/db/types.py                               — PersistenceResult / SaveStatus (50줄)
backend/fastapi/db/supabase_client.py                     — get_supabase_client() graceful (45줄)
backend/fastapi/db/repositories/__init__.py               — exports (8줄)
backend/fastapi/db/repositories/video_project.py          — insert_video_project (60줄)
backend/fastapi/db/repositories/plan_candidate.py         — insert_plan_candidate (75줄)
backend/fastapi/db/migrations/001_init.sql                — video_projects + plan_candidates DDL (55줄)
backend/fastapi/tests/test_db.py                          — 9 케이스 (220줄)
eval/qa_reports/phase-1-slice-5_2026-05-26.md             — 본 문서
```

### 4.2 수정 파일 (8개)

```
backend/fastapi/__init__.py                  — version 0.5.0, Layout docstring 갱신 (db/ 추가)
backend/fastapi/main.py                      — version 0.5.0, /health slice="5", description 갱신
backend/fastapi/config.py                    — supabase_url + supabase_anon_key 필드 추가
backend/fastapi/.env.example                 — Supabase 섹션 활성화 (주석 갱신)
backend/fastapi/requirements.txt             — supabase>=2.7 추가
backend/fastapi/schemas/output.py            — Meta.project_id Optional 추가 + Meta.make(request_id, project_id) 시그니처 확장
backend/fastapi/routers/generate.py          — db.save_video_planning 호출 + meta.project_id 주입 + validation.checks.db_persistence + request_id 통일
backend/fastapi/tests/conftest.py            — mock_db_save_ok / mock_db_save_fail / mock_db_save_skipped + mock_pipeline_ok에 DB save_ok 체이닝
backend/fastapi/tests/test_e2e_slice1.py     — Slice 5 e2e 4 케이스 추가 + 기존 RAG/Critic 케이스에 mock_db_save_ok 인자 추가
```

### 4.3 코드 통계

```
신규 (db module + test):     ~625줄
수정 (router + schema + config + tests): ~165줄 차이
검증 자동화:                  pytest 62 케이스 (Slice 4의 49 + 신규 9 db + 4 e2e db)
회귀 통과율:                  100% (49/49 기존 + 13/13 신규)
```

### 4.4 build 파일 인벤토리 (read-only 점검 통과)

```
✅ docs/contracts/                                  0줄 수정
✅ ai_system/                                       0줄 수정
✅ knowledge/                                       0줄 수정
✅ apps/web/                                        0줄 수정 (parallel sub-agent의 Slice 7 영역)
✅ backend/fastapi/agents/intent.py                 0줄 수정 (Slice 2 안정)
✅ backend/fastapi/agents/planning.py               0줄 수정 (Slice 2/4 안정)
✅ backend/fastapi/agents/critic.py                 0줄 수정 (Slice 3 안정)
✅ backend/fastapi/rag/*                            0줄 수정 (Slice 4 안정)
✅ backend/fastapi/tests/test_intent.py             0줄 수정
✅ backend/fastapi/tests/test_critic.py             0줄 수정
✅ backend/fastapi/tests/test_planning.py           0줄 수정
✅ backend/fastapi/tests/test_rag_fallback.py       0줄 수정
✅ phases/active/phase-1-mvp-basic-flow/*.md         0줄 수정
✅ PROJECT_STATE.md / PHASE_REGISTRY.md             0줄 수정 (main session 관리)
✅ eval/INDEX.md / eval/golden_set.md / failure_cases.md  0줄 수정 (read-only)
```

---

## 5. 후속 조치 / Phase 1 종료 진입 가이드

### 5.1 본 Slice에서 미해결

- **실 Supabase 호출 미검증**: Slice 5는 mock 검증만. 사용자가 `.env` + Supabase 프로젝트 +
  `001_init.sql` migration 실행 후 1회 라이브 호출 검증 권장 (Phase 1 종료 smoke test).
- **plan_options vs plan_candidates 명칭 불일치 (contract deviation)**: db_schema.md §4.2 는
  `plan_options` 명칭 사용. 본 Slice는 work_plan.md Slice 5 명칭 `plan_candidates` 채택.
  → contract-change Skill 절차로 Phase 1 종료 시 정합 권고 (또는 Phase 2 migration).
- **video_projects 컬럼 단순화**: db_schema.md §3.5는 series_id NOT NULL + title + short_idea +
  mode + one_line_direction + status 필드. 본 Slice는 익명 + Discovery/Quick 미도입이라 input_text
  단일 필드. Phase 2 (Discovery/Quick UX 도입) 시 ALTER 추가 권고.
- **agent_io_logs 미도입**: Slice 4 인계 §5.3(c) 의 fallback_reason 추적은 Phase 2+에서 별도 테이블.
- **RLS 정책 미설정**: 익명 저장이므로 RLS 자체가 없는 상태. Phase 5 Auth 도입 시 동시에 정책 정의 필수.
- pytest DeprecationWarning: `HTTP_422_UNPROCESSABLE_ENTITY` (Slice 1부터 누적). Starlette 신버전 도입 시점 일괄 교체.

### 5.2 Slice 6 / Slice 7 / Phase 1 종료 진입 권한

```
✅ Slice 6 (Next.js 진입 UI) → 별도 sub-agent 가 작업 중 (apps/web/).
   백엔드는 Slice 5 종료로 안정화. apps/web/lib/types.ts 에서 meta.project_id Optional 노출 권고.

✅ Slice 7 (진행 stepper + 오류 카드 + PWA manifest) → 별도 sub-agent 가 작업 중.
   백엔드 변경 0건 — frontend 만 작업.

✅ Phase 1 종료 진입 권한 부여 (백엔드 기준)
   → work_plan.md §"Phase 1 종료 직전 (Slice 7 완료 후)" 8단계 smoke test 진입 가능.
```

### 5.3 Phase 1 종료 직전 smoke test 8단계 (assumptions.md §4.4 + work_plan.md)

본 Slice 종료 시점에 Phase 1 종료 진입 권한이 부여된다. Slice 6/7 완료 후 다음 절차로 진행:

```
1. (운영자) Supabase 프로젝트 생성 + .env 에 SUPABASE_URL / SUPABASE_ANON_KEY / DATABASE_URL 입력
2. (운영자) Supabase Dashboard SQL Editor 에서 backend/fastapi/db/migrations/001_init.sql 실행
3. (운영자) Phase 1 종료 직전: qa-check Skill 전체 10 카테고리 실행 (구현 + Simplicity)
4. (운영자) golden_set 11개 + failure_cases 5개 회귀 (구현 + 품질)
5. (운영자) 사람 리뷰 5 케이스 (eval/qa_reports/phase-1-human-review_{date}.md)
6. (운영자) 라이브 smoke: curl POST + DB row 직접 확인 (Supabase Dashboard 1회)
7. (운영자) eval/qa_reports/phase-1-final_{date}.md 작성
8. (운영자) meta/retrospectives/phase-1.md 작성 (meta-retrospective Skill 호출)
```

### 5.4 Phase 2 진입 사전 점검 체크리스트 (Phase 1 종료 직전)

- [ ] golden_set 11개 회귀 모두 통과 (mock + 1회 라이브)
- [ ] failure_cases 5개 모두 Critic이 flag
- [ ] DB 실 호출 1회 검증 (video_projects + plan_candidates row 생성 + Supabase Dashboard 확인)
- [ ] meta.project_id 가 실제 video_projects.id 와 일치하는지 확인
- [ ] db_schema.md §3.5 (`plan_options`) vs Slice 5 (`plan_candidates`) 명칭 결정 (contract-change Skill)
- [ ] series_id / brand / domain 4계층 도입 시점 결정 (Phase 2 우선 vs Phase 3+)
- [ ] Auth 도입 Phase 5의 ALTER migration 초안 작성 (002_auth.sql)

### 5.5 Slice 5 핵심 컨벤션 인계 (Phase 2+ 사용)

Phase 2+에서 본 Slice 의 다음 인터페이스를 그대로 활용:

- `save_video_planning(...)` orchestrator 함수: 입력 시그니처에 `user_id` 추가 + RLS service_role
  client 분기 추가 시점이 Phase 5.
- `PersistenceResult.error_reason` 자유 문자열: Phase 2+ enum 화 권장
  (`"video_projects_insert_failed" | "plan_candidates_insert_failed" | "unexpected_exception"` 등).
- `raw_llm_json` JSONB 통합 저장: Phase 5+ 또는 100K row 누적 시 정규화 권고 (plan_candidate_rag_usage).
- `db/migrations/` 디렉토리: 002_*.sql 부터 sequential 번호 + ROLLBACK 주석 권장.

---

## 6. 변경 이력

- 2026-05-26: Phase 1 Slice 5 완료, Supabase persistence + graceful failure 추가.
  pytest 62/62 통과 (Slice 4의 49 + Slice 5의 13). validation.checks에 `db_persistence` 항목 추가
  (6번째 check). meta.project_id Optional 노출. DB 미연결 / 패키지 미설치 / insert 예외 /
  빈 응답 / 부분 실패 / 예상치 못한 예외 6 가지 경로 모두 사용자 차단 없이 200 응답 보장.
  Slice 4 RAG 의 graceful sub-system 패턴을 DB 모듈에도 동일 적용 → Phase 1 컨벤션 통일.
