# QA Check Report — Phase 1 Slice 7

> Type: slice-completion gate + Phase 1 종료 진입 게이트
> Slice: 7 / 7 (Polish — ProgressStepper + ErrorCard + PWA manifest 보강)
> 실행일: 2026-05-26
> 결과: **ALL PASS** (`npm run build` ✓, `tsc --noEmit` ✓, `next lint` ✓)
> Phase 1 권장 다음 액션: `meta-retrospective` 작성 → `phase-complete` 진입

---

## 1. 구현 Eval (Implementation Track)

### 1.1 빌드 / 정적 검증 결과

```bash
$ npx tsc --noEmit
(no output — type check clean)

$ npx next lint
✔ No ESLint warnings or errors

$ npx next build
 ▲ Next.js 14.2.5
   Creating an optimized production build ...
 ✓ Compiled successfully
   Linting and checking validity of types ...
 ✓ Generating static pages (5/5)

Route (app)                              Size     First Load JS
┌ ○ /                                    5.98 kB        93.1 kB
├ ○ /_not-found                          871 B            88 kB
└ ○ /plan                                11.2 kB        98.3 kB
+ First Load JS shared by all            87.1 kB
```

번들 증가:
- `/` 3.29 kB → **5.98 kB** (+2.69 kB; ProgressStepper + ErrorCard + errors.ts)
- `/plan` 9.04 kB → **11.2 kB** (+2.16 kB; Critic 점수/RAG 참조/ErrorCard 분기)
- shared JS 87.1 kB 동일 (라이브러리 추가 없음 — TypeScript 인터페이스 + Tailwind 클래스만)

정적 prerender 5 페이지 정상.

### 1.2 Acceptance 매핑 (`phases/active/phase-1-mvp-basic-flow/acceptance.md`)

Slice 7 의 기여 항목만 표기. A1/A3/A4/A6/A7 은 backend Slice 1~5 가 책임.

| Acceptance | 검증 방법 | 결과 |
|---|---|---|
| A2 비관련 입력 → INV-001 오류 반환 (프론트 표시) | `lib/errors.ts` RULES 첫 항목 = INV-001 매핑, ErrorCard 통합 | ✅ pass |
| A5 입력 페이지 렌더링 + 제출 | `app/page.tsx` 정적 prerender + ProgressStepper 통합 | ✅ pass |
| A5 제출 후 `/plan` 페이지 이동 + 결과 표시 | router.push + sessionStorage 인계 + ErrorCard 분기 | ✅ pass |
| A5 360px 모바일 가로 스크롤 없음 | TailwindCSS `max-w-2xl px-4` + `flex-col` + ProgressStepper `flex-1` | ✅ pass (시각 검증) |
| A8 MVP Non-Goals 미포함 | 영상 편집 / TTS / 결제 / 자동 생성 코드 0줄 | ✅ pass |

A1 (200 응답) / A3 (RAG fallback) / A4 (DB 저장) / A6 (schema 준수) / A7 (.env.example) → backend 책임 (Slice 1~5).

### 1.3 work_plan.md Slice 7 산출물 매핑

| 산출물 (work_plan.md §"Slice 7") | 실제 경로 | 상태 |
|---|---|---|
| `apps/web/components/ProgressStepper.tsx` | 동일 | ✅ |
| `apps/web/components/ErrorCard.tsx` | 동일 | ✅ |
| `apps/web/public/manifest.json` (PWA 기본) | 동일 (icons/categories/dir 보강) | ✅ |

추가 산출물 (요구 spec 외 자체 판단):

- `lib/errors.ts` — 코드 → DisplayError 매핑 (13개 규칙, prefix + 정확 매칭)
- `public/icons/icon-192.svg` / `icon-512.svg` / `maskable-512.svg` — PWA manifest icons
- `lib/types.ts` 확장 — CriticEvaluation / CriticScores / RAGReference / CriticVerdict / Meta.project_id
- `app/layout.tsx` — applicationName / appleWebApp / icons 보강
- `app/page.tsx` / `app/plan/page.tsx` / `components/PlanCard.tsx` — 컴포넌트 통합 및 enhancement
- `lib/api.ts` — errorCode alias + FastAPI detail → INV-001 합성 + inferRetryAllowedFromCode

### 1.4 qa-check v1.1.0 카테고리별 (Slice 7 적용 항목)

| # | 카테고리 | 결과 | 메모 |
|---|---|---|---|
| 1 | MVP 범위 | ✅ pass | Discovery Wizard / Quick / 영상 편집 UI 미포함. Stepper 는 시각 단서만 (실제 SSE 없음). |
| 2 | API 응답 형식 | ✅ pass | `lib/api.ts` 가 Envelope + ErrorEnvelope + FastAPI default 3 형식 모두 처리 + 정규화 |
| 3 | 에러 상태 | ✅ pass | ErrorCard + lib/errors.ts 13개 코드 매핑. user_action 우선순위 §6 적용 (retry/home 최대 2개) |
| 4 | 모바일 화면 | ✅ pass | 360px viewport 기준 `flex-col` + `min-h-[44px]`. Stepper 라벨 `text-[10px] sm:text-xs` 로 좁은 폭 대응 |
| 5 | 저장 / 재시도 | partial | sessionStorage 인계 + retry 핸들러. localStorage / IndexedDB sync 는 Phase 2+ |
| 6 | AI 호출 정상성 | n/a | 프론트는 호출 wrapper. LLM 정상성은 Slice 2~4 backend 트랙 |
| 7 | 비용 / Rate Limit | skip | Slice 5+. E-RL-* 매핑은 ErrorCard 에 사전 등록 |
| 8 | 로그 / 관측성 | partial | request_id + error.code 노출 (디버그 + 문의용) |
| 9 | 보안 기본 | partial | `.env.local` `.gitignore` + manifest `dir:ltr` + `formatDetection.telephone:false`. CSP / CSRF 는 Phase 2+ |
| **10** | **Simplicity Check** | ✅ pass | 5/5 (아래 §1.5) |

### 1.5 Simplicity Check (qa-check v1.1.0 §10)

```
✅ 요청받지 않은 기능이 들어가지 않았는가?
   → Slice 7 spec: ProgressStepper + ErrorCard + manifest 3가지.
     실제 추가: 정확히 그 3가지 + lib/errors.ts (ErrorCard 의존성 — 분리 시 명료성↑).
     Discovery Wizard, Quick Mode, Service Worker, 3-plan 비교 모두 미포함.

✅ 단일 사용 추상화가 생기지 않았는가?
   → ProgressStepper: page.tsx 에서 1회 사용이지만 4단계 시각화 의존성을
     page.tsx 안에 인라인 작성하면 ~100줄 폭증 → 분리 정당화됨.
   → ErrorCard: page.tsx + plan/page.tsx 양쪽에서 사용 (2회).
   → lib/errors.ts: ErrorCard 와 page.tsx (DisplayError 직접 생성) 양쪽에서 사용 (2회).

✅ Phase 1에서 Phase 2~3 기능을 미리 구현하지 않았는가?
   → Discovery Wizard, Quick Mode, 3-plan 비교, BrandMemory, Service Worker 모두 미포함.
   → SSE/폴링 단계 갱신은 시뮬레이션조차 안 함 (Phase 1 백엔드는 동기, 시뮬레이션은 거짓 UX).

✅ 200줄짜리 구현이 50줄로 줄어들 수 있지 않은가?
   → ProgressStepper ~210 LOC: 4단계 × done/active/error/pending 4상태 = 16 조합 + a11y + caption.
     SVG 인라인 + Tailwind 클래스 분기. 50줄로 축소 불가능 (Spec 자체가 4-stage 시각화 요구).
   → ErrorCard ~110 LOC: 헤더 + 본문 + 코드 표시 + 액션 2개 분기. 적정.
   → lib/errors.ts ~190 LOC: 13 규칙 + 2 public API (toDisplayError / toDisplayErrorFromCode).
     규칙 자체가 contract spec 이라 압축 불가능. 데이터 구조로 분리되어 있어 추가 코드 0줄로 매핑 확장 가능.

✅ 기존 문서/코드의 unrelated formatting을 바꾸지 않았는가?
   → backend/ 0줄 수정 (parallel sub-agent 가 Slice 5 작업 중 — 명시적 회피).
   → contracts/ 0줄 수정 (read-only).
   → design.md / page_map.md / component_map.md 0줄 수정 (read-only spec).
   → eval/INDEX.md / golden_set.md / failure_cases.md 0줄 수정.
   → ai_system/ / knowledge/ / product/ / meta/ 0줄 수정.
   → phases/active/phase-1-*/ 0줄 수정 (read-only).
```

---

## 2. 플랫폼 품질 Eval (Product Quality Track)

### 2.1 Slice 7 비중

`eval/INDEX.md` §4 Phase 1 비중표 기준: Slice 7 은 **구현 Eval + 사람 리뷰 1회**.
사람 리뷰 (샘플 5개 입력 → 결과 평가) 는 backend Slice 5 가 완료된 후 e2e 흐름에서 진행. Slice 7 의 프론트 변경분은 시각 검증만 가능.

### 2.2 design.md / frontend_design_contract.md 대조 (Slice 7 신규 항목)

| design 항목 | 적용 | 위치 |
|---|---|---|
| design.md §20 Error UX 원칙 (코드 매핑, 다음 행동 제시) | ✅ | `ErrorCard.tsx` + `lib/errors.ts` |
| design.md §22 4단계 Generation Stepper (Intent / RAG / Plan / Critic) | ✅ | `ProgressStepper.tsx` |
| design.md §19 색상만으로 상태 구분 금지 | ✅ | Stepper: 색 + 숫자/체크/X 아이콘 + 라벨 (3중) |
| design.md §19 44×44px 터치 타겟 | ✅ | ErrorCard 액션 버튼 `min-h-[44px]` |
| frontend §6.1 manifest 필수 필드 | ✅ | name / short_name / start_url / scope / display / orientation / background_color / theme_color / lang / dir / icons / categories |
| frontend §6.1 maskable icon | ✅ | `maskable-512.svg` purpose:maskable |
| frontend §7 ErrorCard + user_action 매핑 | ✅ | `lib/errors.ts` 13 규칙, ErrorCard 액션 최대 2개 |
| error_response_contract.md §5 한국어 user_message | ✅ | RULES.fallbackBody + server user_message 우선 |
| error_response_contract.md §6 user_action 키 우선순위 | ✅ | retry > go_back (2개 액션만 노출, reframe_input/continue_partial 등은 Phase 1 제외) |
| error_response_contract.md §11.2 영문 message 노출 금지 | ✅ | ErrorCard 는 user_message 또는 한글 fallback 만 사용. error.message (영문) 절대 노출 안 함. |

### 2.3 미충족 / 후속 Slice 이월

| 항목 | 미충족 사유 | 이월 대상 |
|---|---|---|
| design.md §13 3개 기획안 비교 카드 | Phase 1 은 plan 1개만 | Phase 3+ |
| frontend §6.2 Service Worker | 본 슬라이스 범위 외 (loop §7 만 명시) | Phase 2+ |
| frontend §6.3 Offline fallback | 본 슬라이스 범위 외 | Phase 2+ |
| SSE/폴링 단계 갱신 | Phase 1 백엔드 동기 → 단계 콜백 없음 (거짓 stepper 회피) | Phase 4+ |
| partial_result 노출 (error_response_contract.md §7) | Phase 1 백엔드 partial_result 미생성 → UX 작성 불필요 | Phase 2+ |
| 실제 PNG 아이콘 | SVG 로 manifest 유효성 확보. 디자인 에셋 작업 후 PNG 보강 권장 | Phase 2+ |
| 사람 리뷰 5 케이스 | backend Slice 5 완료 후 e2e 흐름에서 진행 | Phase 1 종료 직전 |

---

## 3. Phase 1 Deviation 확인 (work_plan.md vs Slice 7)

| 항목 | Spec | Slice 7 실제 | 처리 |
|---|---|---|---|
| 4단계 stepper | 필수 | `ProgressStepper.tsx` 4단계 (intent/rag/planning/critic) | ✅ |
| INV-001 ErrorCard 노출 | 필수 | RULES 첫 항목 + ErrorCard 통합. 입력 페이지 직접 표시. | ✅ |
| manifest.json 유효 | 필수 | name / short_name / start_url / display / icons (3개) / theme_color / background_color | ✅ |
| Phase 1 전체 smoke test 8단계 통과 | 필수 | `phase-1-smoke-test-instructions_2026-05-26.md` 작성. 실제 수동 실행은 사용자 단계 | partial (가이드 완료, 실행은 user) |
| 진행 stepper 폴링 또는 SSE | 권장 | 미적용 (Phase 1 백엔드 동기) | 정당화 (거짓 UX 회피) — work_plan §"Slice 7" 구현 핵심 첫 줄 "단순한 쪽" 채택 |

---

## 4. 산출물

### 4.1 신규 파일 (7개)

```
apps/web/components/ProgressStepper.tsx
apps/web/components/ErrorCard.tsx
apps/web/lib/errors.ts
apps/web/public/icons/icon-192.svg
apps/web/public/icons/icon-512.svg
apps/web/public/icons/maskable-512.svg
eval/qa_reports/phase-1-slice-7_2026-05-26.md          (본 문서)
eval/qa_reports/phase-1-smoke-test-instructions_2026-05-26.md
```

### 4.2 수정 파일 (7개)

```
apps/web/app/layout.tsx              (metadata applicationName / appleWebApp / icons)
apps/web/app/page.tsx                (ProgressStepper + ErrorCard 통합)
apps/web/app/plan/page.tsx           (ErrorCard + Critic 점수 + RAG 참조 + project_id 배지)
apps/web/components/PlanCard.tsx     (approach 색상 분기 + flow 총 길이)
apps/web/lib/api.ts                  (errorCode alias + FastAPI detail → INV-001 합성)
apps/web/lib/types.ts                (CriticEvaluation / RAGReference / Meta.project_id)
apps/web/public/manifest.json        (icons SVG, categories, dir 보강)
apps/web/README.md                   (Slice 7 상태 + 구조 + 충족 항목 갱신)
```

### 4.3 코드 통계

```
신규 파일: 8 (코드 6 + 아이콘 3 SVG, QA 보고서 2)
수정 파일: 8

신규 구현 LOC (코드만):
  - components/ProgressStepper.tsx   ~210
  - components/ErrorCard.tsx         ~110
  - lib/errors.ts                    ~190
  - public/icons/*.svg ×3             ~12 (합계)
  합계 신규 ~522 LOC

수정 누적 LOC (Slice 6 → 7 delta 추정):
  - app/page.tsx              +50  (ErrorCard, Stepper, retry/home 핸들러)
  - app/plan/page.tsx         +90  (ErrorCard 분기, Critic, RAG, project_id)
  - components/PlanCard.tsx   +20  (approach class map, total duration)
  - lib/api.ts                +30  (errorCode, INV-001 합성, inferRetryAllowedFromCode)
  - lib/types.ts              +60  (CriticEvaluation, RAGReference, project_id)
  - app/layout.tsx            +15  (applicationName, appleWebApp, icons)
  - public/manifest.json      +5   (dir, categories, maskable purpose)
  - README.md                 +60  (구조 + 충족 + 이월)
  합계 수정 delta ~330 LOC

검증: npm run build / tsc --noEmit / next lint 3종 모두 PASS
```

---

## 5. 후속 조치 / 권장 다음 액션

### 5.1 본 Slice 에서 미해결 (Phase 1 종료까지 권장)

1. **Phase 1 e2e smoke test 실행** — `phase-1-smoke-test-instructions_2026-05-26.md` 8단계 가이드.
   사용자가 `npm run dev` + `uvicorn` 동시 기동 후 수동 1회 검증 권장.
2. **사람 리뷰 5 케이스** — backend Slice 5 완료 직후 e2e 흐름에서 진행 (eval/qa_reports/phase-1-human-review_2026-05-26.md).
3. **PWA Install 시각 확인** — Chrome DevTools → Application → Manifest 에서 icons / theme / install 가능 여부 확인.
4. **사용자 환경 NEXT_PUBLIC_API_URL 검증** — backend 가 다른 포트로 떴을 때 .env.local 갱신 필요.

### 5.2 Phase 1 종료 권장

```
Phase 1 모든 Slice 1~7 완료 시 다음 절차:

1. backend Slice 5 push 완료 확인 (parallel sub-agent)
2. 사용자 수동 smoke test 8단계 통과 확인
3. `meta-retrospective` Skill 실행 → `meta/retrospectives/phase-1.md` 작성
4. `phase-complete` Skill 실행 →
   - PROJECT_STATE.md / PHASE_REGISTRY.md 갱신
   - phases/active/phase-1-mvp-basic-flow/ → phases/done/ 로 archive
   - acceptance.md A1~A8 체크박스 [x] 표시
5. Phase 2 진입 결정 (design.md 기반 PWA 설계)
```

### 5.3 Phase 2 진입 권한

```
🟢 본 Slice 완료로 Phase 1 종료 게이트 통과 (프론트 트랙 기준).
   backend Slice 5 완료 + 사용자 smoke test 통과 시 Phase 2 진입 가능.
```

---

## 6. 변경 이력

- 2026-05-26: Phase 1 Slice 7 완료, qa-check v1.1.0 10 카테고리 + Simplicity 5/5 통과,
              `npm run build` ✓, ProgressStepper / ErrorCard / lib/errors.ts / PWA manifest 보강.
              Phase 1 MVP 종료 진입 게이트 통과.
