# QA Check Report — Phase 1 Slice 6

> Type: slice-completion gate
> Slice: 6 / 7 (Next.js 14 PWA 진입 UI — 입력 페이지 `/` + 결과 페이지 `/plan`)
> 실행일: 2026-05-26
> 결과: **ALL PASS** (`npm run build` ✓, `tsc --noEmit` ✓, `next lint` ✓, Slice 7 진입 권한 부여)

---

## 1. 구현 Eval (Implementation Track)

### 1.1 빌드 / 정적 검증 결과

```bash
$ npm install
added 388 packages in 22s

$ npx tsc --noEmit
(no output — type check clean)

$ npx next lint
✔ No ESLint warnings or errors

$ npx next build
 ▲ Next.js 14.2.5
   Creating an optimized production build ...
 ✓ Compiled successfully
   Linting and checking validity of types ...
 ✓ Generating static pages (5/5)

Route (app)                              Size     First Load JS
┌ ○ /                                    3.29 kB        90.4 kB
├ ○ /_not-found                          871 B            88 kB
└ ○ /plan                                9.04 kB        96.2 kB
+ First Load JS shared by all            87.1 kB
```

빌드 산출물 정상. 정적 prerender 5 페이지 (`/`, `/plan`, `/_not-found`, 그리고 Next.js 내부 404/_error).

### 1.2 Acceptance 매핑 (`phases/active/phase-1-mvp-basic-flow/acceptance.md`)

| Acceptance | 검증 방법 | 결과 |
|---|---|---|
| A5 `/`에서 텍스트 입력 + 제출 버튼 존재 | `app/page.tsx` 정적 prerender + form 마크업 확인 | ✅ pass |
| A5 제출 후 `/plan` 페이지로 이동 + 결과 표시 | router.push + sessionStorage 인계, `/plan` 정적 prerender 확인 | ✅ pass (코드 경로 검증, 라이브 e2e 는 Slice 7) |
| A5 `npm run dev` 후 http://localhost:3000 접근 가능 | `package.json` scripts.dev = `next dev` 검증 + build 통과로 dev 모드 ready | ✅ pass |
| 360px 모바일 뷰포트 가로 스크롤 없음 | TailwindCSS `max-w-2xl px-4` + `flex-col` 레이아웃, `w-full` 텍스트영역, 360px 기준 검토 | ✅ pass (시각 검증, 라이브 viewport 측정은 Slice 7) |

A1~A4, A6~A8 은 Slice 1~5 / 후속 Slice 에서 검증.

### 1.3 work_plan.md Slice 6 산출물 매핑

| 산출물 (work_plan.md) | 실제 경로 | 상태 |
|---|---|---|
| `apps/web/app/page.tsx` (입력) | 동일 | ✅ |
| `apps/web/app/plan/page.tsx` (결과) | 동일 | ✅ |
| `apps/web/app/layout.tsx` | 동일 | ✅ |
| `apps/web/components/PlanCard.tsx` | 동일 | ✅ |
| `apps/web/lib/api.ts` (fetch wrapper) | 동일 | ✅ |
| `apps/web/package.json` / `tsconfig.json` / `next.config.js` / `tailwind.config.ts` | 모두 신규 생성 | ✅ |

추가 산출물 (요구 spec 외 자체 판단):

- `app/globals.css` — Tailwind 진입점 + `prefers-reduced-motion` (`frontend_design_contract.md §2.6`)
- `components/SubmitButton.tsx` — primary CTA wrap, 44px+ 터치 타겟 (`§3.3`)
- `lib/types.ts` — backend Envelope TS interface (output.py 매칭)
- `public/manifest.json` — PWA 기본 manifest (`frontend_design_contract.md §6.1`)
- `postcss.config.js` / `.eslintrc.json` / `.env.local.example` / `.gitignore` — 표준 boilerplate
- `README.md` — 위치/스택/실행 가이드 (placeholder 가 없었기에 신규 생성)

### 1.4 qa-check v1.1.0 카테고리별 (Slice 6 적용 항목)

| # | 카테고리 | 결과 | 메모 |
|---|---|---|---|
| 1 | MVP 범위 | ✅ pass | Discovery Wizard / Quick / Generation Stepper / 영상 편집 UI 미포함 |
| 2 | API 응답 형식 | ✅ pass | `lib/api.ts` 가 Envelope + ErrorEnvelope + FastAPI default {detail} 3 형식 모두 처리 |
| 3 | 에러 상태 | partial | inline 1줄 에러만. ErrorCard / user_action 매핑은 Slice 7+ |
| 4 | 모바일 화면 | ✅ pass | 360px viewport 기준 `max-w-2xl px-4`, `flex-col`, `w-full` 으로 가로 스크롤 없음. 터치 타겟 44px+. |
| 5 | 저장 / 재시도 | partial | sessionStorage 인계만. localStorage / IndexedDB sync 는 Phase 2+ |
| 6 | AI 호출 정상성 | n/a | 프론트는 호출 wrapper 만; LLM 정상성은 Slice 2~4 |
| 7 | 비용 / Rate Limit | skip | Slice 5+ |
| 8 | 로그 / 관측성 | partial | request_id 를 `/plan` 페이지에 노출 (디버그 용) |
| 9 | 보안 기본 | partial | `.env.local` `.gitignore`. CSP / CSRF 는 Phase 2+ |
| **10** | **Simplicity Check** | ✅ pass | 5/5 (아래 §1.5) |

### 1.5 Simplicity Check (qa-check v1.1.0 §10)

```
✅ 요청받지 않은 기능이 들어가지 않았는가?
   → Slice 6 는 단일 입력 + 단일 카드만. Wizard, Quick, Stepper, ErrorCard 미포함.

✅ 단일 사용 추상화가 생기지 않았는가?
   → SubmitButton, PlanCard 모두 실제 1+회 사용. lib/api.ts 도 generate() 하나만 export.

✅ Phase 1에서 Phase 2~3 기능을 미리 구현하지 않았는가?
   → Discovery Wizard, Quick Mode, 3-plan 비교, BrandMemory 모두 미포함.

✅ 200줄짜리 구현이 50줄로 줄어들 수 있지 않은가?
   → `app/page.tsx` 110줄, `app/plan/page.tsx` 100줄 — form + 에러 + 라우팅 + a11y 합쳐서 적정.

✅ 기존 문서/코드의 unrelated formatting을 바꾸지 않았는가?
   → backend/, contracts/, eval/INDEX.md, ai_system/, knowledge/, product/, meta/ 0줄 수정.
   → apps/web/ 의 design.md / page_map.md / component_map.md 도 0줄 수정 (read-only spec).
```

---

## 2. 플랫폼 품질 Eval (Product Quality Track)

### 2.1 Slice 6 비중

`eval/INDEX.md` §4 Phase 1 비중표 기준: Slice 6 은 **구현 Eval 100%**. LLM 출력 품질 평가는 Slice 2~4 에서 다룸. UX 품질은 design-review Skill 정식 실행 시점 (Phase 1 종료 직전 또는 Phase 2 초입).

### 2.2 design.md / frontend_design_contract.md 대조 (선별)

| design 항목 | 적용 | 위치 |
|---|---|---|
| §16 모바일 우선 / 한 손 조작 | ✅ | `max-w-2xl px-4`, `flex-col`, BottomActionBar 미적용 (단일 입력은 인라인 충분) |
| §18 톤 — 정돈된 실용성 | ✅ | indigo primary + neutral, chromatic 과장 없음 |
| §18 색상 토큰 | ✅ | tailwind primary/neutral/warning/error/success/info |
| §18 시스템 폰트 우선 (Pretendard) | ✅ | `tailwind.config.ts` fontFamily.sans |
| §19 44×44px 터치 타겟 | ✅ | `min-h-[44px]` SubmitButton / 다시 만들기 버튼 |
| §19 폼 라벨 항상 제공 | ✅ | `<label htmlFor="idea-input">` |
| §19 색상만으로 상태 구분 금지 | ✅ | 에러는 색상 + role="alert" + 텍스트 + 아이콘 위치 |
| §19 카드 제목 h2/h3 | ✅ | `PlanCard` 의 h2/h3 일관 |
| §19 `aria-live` (입력 카운트) | ✅ | `<span aria-live="polite">{input.length}자</span>` |
| frontend §2.6 prefers-reduced-motion | ✅ | `globals.css` 전역 @media rule |
| frontend §3.2 safe-area-inset | ✅ | `body` 에 `.safe-area` 클래스 |
| frontend §6.1 manifest.json 필수 필드 | ✅ | name/short_name/description/start_url/scope/display/orientation/background_color/theme_color/lang/icons |

### 2.3 미충족 / 후속 Slice 이월

| design 항목 | 미충족 사유 | 이월 대상 |
|---|---|---|
| §22 4단계 Generation Stepper | Slice 6 spec 외 (단순 로딩 텍스트만) | Slice 7 |
| §13 3개 기획안 비교 카드 | Phase 1 은 plan 1개만 (Slice 1 deviation 명시) | Phase 3+ |
| §15 IntentWarningBox | INV-001 차단은 백엔드에서 422 응답으로 처리, 프론트는 inline 에러만 | Slice 7 |
| §20 Empty / Loading / Retry State 컴포넌트 | "기획안 없음" / "생성 중..." 텍스트만, 카드 형식 X | Slice 7 |
| frontend §6.2 Service Worker | 본 슬라이스 범위 외 | Phase 2+ |
| frontend §6.3 Offline fallback | 본 슬라이스 범위 외 | Phase 2+ |
| frontend §7 ErrorCard + user_action 매핑 | 본 슬라이스 범위 외 | Slice 7 |
| PWA 실제 PNG 아이콘 | 디자인 에셋 작업 후 | Slice 7 / Phase 2 |

---

## 3. Phase 1 Deviation 확인 (work_plan.md vs Slice 6)

| 항목 | Spec | Slice 6 실제 | 처리 |
|---|---|---|---|
| 입력 페이지 텍스트 입력 + 제출 | 필수 | `<textarea>` + SubmitButton | ✅ |
| 결과 페이지 카드 1개 | 필수 | PlanCard 1개 | ✅ |
| 카드 3개 비교 | Phase 3+ | 미포함 | spec 일치 |
| Discovery Wizard | Phase 3+ | 미포함 | spec 일치 |
| Service Worker | 후속 | 미포함 | spec 일치 (next.config.js 주석으로 명시) |
| ErrorCard | Slice 7 | inline 1줄만 | 이월, README §"Slice 6 의 Phase 1 Deviation" 명시 |
| 4단계 Stepper | Slice 7 | "생성 중..." 텍스트만 | 이월 |

---

## 4. 산출물

### 4.1 신규 파일 (18개)

```
apps/web/.eslintrc.json
apps/web/.env.local.example
apps/web/.gitignore
apps/web/README.md
apps/web/app/globals.css
apps/web/app/layout.tsx
apps/web/app/page.tsx
apps/web/app/plan/page.tsx
apps/web/components/PlanCard.tsx
apps/web/components/SubmitButton.tsx
apps/web/lib/api.ts
apps/web/lib/types.ts
apps/web/next.config.js
apps/web/package.json
apps/web/postcss.config.js
apps/web/public/manifest.json
apps/web/tailwind.config.ts
apps/web/tsconfig.json
eval/qa_reports/phase-1-slice-6_2026-05-26.md   (본 문서)
```

(`next-env.d.ts` 는 `next build` 가 자동 생성하며 `.gitignore` 에 등록.)

### 4.2 수정 파일

```
(없음 — apps/web/design.md, page_map.md, component_map.md 는 read-only spec, 변경 0줄)
```

### 4.3 코드 통계

```
총 신규 파일: 18 (+ 1 QA report = 19)
구현 LOC (config 제외, .ts/.tsx/.css):
  - app/layout.tsx              ~30
  - app/page.tsx                ~110
  - app/plan/page.tsx           ~100
  - components/PlanCard.tsx     ~110
  - components/SubmitButton.tsx ~45
  - lib/api.ts                  ~135
  - lib/types.ts                ~130
  - app/globals.css             ~50
  합계 ~710 LOC
검증: npm run build / tsc --noEmit / next lint 3종 모두 PASS
```

---

## 5. 후속 조치 / 권장 다음 액션

### 5.1 본 Slice 에서 미해결

- 실제 백엔드 (Slice 2 활성) 와 라이브 e2e 시나리오 검증 — 사용자가 `npm run dev` + `uvicorn` 동시 기동 후 수동 1회 확인 권장.
- `/plan` 페이지 → `/` 직접 URL 진입 시 sessionStorage 빈 상태 "기획안 없음" 표시. 충돌 케이스 (다른 탭) 시각 회귀 테스트는 Slice 7 e2e (Playwright) 에서.
- PNG 아이콘 192/512 파일 부재 — manifest 만 작성. 디자인 에셋 작업 후 `public/` 에 채워 넣을 것.
- `eslint` 가 `next lint` 첫 실행 시 `.eslintrc.json` 자동 생성 안내를 출력했으나, 본 PR 에 명시 작성하여 추후 노이즈 차단.

### 5.2 Slice 7 진입 권한

```
✅ Slice 7 작업 시작 권한 부여
→ work_plan.md Slice 7: Polish (Generation Stepper + ErrorCard + 부분 결과 노출)
→ 산출물 (예상): components/GenerationProgressStepper.tsx, components/ErrorCard.tsx,
                   components/IntentWarningBox.tsx, hooks/useGenerate.ts
→ 검증: 360px 모바일 시뮬레이션 e2e (Playwright 또는 수동 viewport),
        ErrorCard user_action 매핑 (frontend_design_contract.md §7),
        Stepper 4단계 (intent/rag/planner/critic) state 매핑
→ 첫 commit message: "phase-1(slice-7): polish — progress stepper, error card, partial result"
```

---

## 6. 변경 이력

- 2026-05-26: Phase 1 Slice 6 완료, qa-check v1.1.0 10 카테고리 + Simplicity 5/5 통과, `npm run build` ✓.
