# Contract Change Log — Phase 9.5 Slice 4 Critic deprecated 0–5 Full 제거 (canonical-only)

> ID: CC-005
> Status: **decided + applied** (2026-05-31, Phase 9.5 Slice 4)
> Date: 2026-05-31
> Decision: Critic deprecated 0–5 Full 제거 (사용자 결정 — ADR-034 Slice 1 선행 승인)
> Author: Claude (Phase 9.5 Slice 4 sub-agent)
> Related contracts: `docs/contracts/output_schema.md` (§9), `docs/contracts/agent_io_contract.md` (§5), `docs/contracts/db_schema.md` (critic_evaluation / quality_scores)
> Related ADR: ADR-034 (`docs/decisions/phase_9_5_critic_deprecated_removal.md`), ADR-018 (canonical 선언), ADR-032 (canonical wiring), ADR-033 (eval baseline)
> Proposal: `meta/proposals/2026-05-31_phase-9.5-slice-4-critic-deprecated-removal.md`
> Skill: contract-change (절차) + eval-run (제거 전/후 baseline) + agent-io-check (drift 검증)

---

## 1. 변경 요약

| 대상 | 변경 |
|---|---|
| `output_schema.md` | §9 header note(ADR-034) + §9.1 Body 스키마에서 deprecated 0–5(scores / overall_score_avg) 제거 + §9.1.2 "제거 완료" 갱신 + §9.2 canonical 검증 정리 + §9.4 select_best fallback 제거 + §9-A.3 canonical-only + §19.2 overall_score(0–1) + §23 v1.2.0 entry. |
| `agent_io_contract.md` | §5 header note(ADR-034) + §5.3 Output canonical-only(deprecated 제거, run_critic 0–5 LLM-facing 불변 명시) + §20 v1.3.0 entry. |
| `db_schema.md` | critic_evaluation JSONB canonical-only note(deprecated 0–5 제거) + quality_scores 는 LLM-facing 0–5 보존 (critic_evaluation canonical 과 구분) note. |

## 2. 코드 영향

```
backend/fastapi/agents/critic.py        — select_best_plan_index deprecated fallback(overall_score_avg/scores/eight_dim_scores + DeprecationWarning) 제거 + import warnings 정리. run_critic 0–5 + normalize_to_canonical 불변.
backend/fastapi/schemas/output.py       — CriticEvaluation deprecated 0–5 필드(scores / overall_score_avg) 제거 + model_config extra='ignore' 명시. CriticScores 모델 유지(run_critic 0–5 typing).
backend/fastapi/routers/generate.py     — Phase 1 endpoint: run_critic → normalize_to_canonical → CriticEvaluation wiring (orchestrator moa_orchestrator.py 와 동일 패턴, ADR-032). deprecated 필드 제거로 canonical 미생성 회귀 방지 (1 import + 1 line).
backend/fastapi/tests/test_critic.py / test_3_plan.py / test_e2e_slice1.py / test_schema_stress.py / test_critic_canonical_wiring.py  — 의도 delta (deprecated 0–5 → canonical 검증).
apps/web/app/plan/[plan_id]/page.tsx / apps/web/app/plan/page.tsx  — critic canonical(overall_score 0–1 → %) 렌더 전환 (PlanCard.tsx 0줄, component_map.md 0줄).
apps/web/lib/types.ts                   — CriticEvaluation deprecated 필드(scores / overall_score_avg) 제거 + target_plan_id Optional 강등.
```

## 3. 회귀 안전 근거

- **run_critic 0–5 출력(LLM-facing P-007 prompt contract) 불변** (NG3) — SYSTEM_PROMPT 0–5 8 dims +
  _derive_verdict + normalize_to_canonical(0–5→0–1 변환) 모두 유지.
- `CriticEvaluation` `extra='ignore'` (Pydantic v2 default 명시) → orchestrator/generate 가 normalize_to_canonical
  산출 verdict dict(0–5 키 병행)를 그대로 넘겨도 0–5 키 무시 + canonical 추출 → 응답 회귀 0.
- Phase 9 wiring(ADR-032) 으로 canonical 항상 populated → deprecated fallback 은 이미 dead code.
- eval-run 제거 전/후 gate 동일 (canonical-only 품질 baseline — Slice 3 안전망).
- 의도 delta 외 baseline test 0 수정.

## 4. 검증 결과

```
eval-run (mock-deterministic, golden_set 11 케이스):
  제거 전 (phase-9.5_pre-removal):  schema 100% / pass 100% / gate PASS / revise mean_delta 0.092 / improved 0.6 / regressed 0.2
  제거 후 (phase-9.5_post-removal): schema 100% / pass 100% / gate PASS / revise mean_delta 0.092 / improved 0.6 / regressed 0.2  → 동일 (회귀 0)
pytest backend/fastapi/tests/: 338 → 339 PASS (의도 delta — test_critic +1 / test_schema_stress +1, 그 외 assertion 전환).
Critic DeprecationWarning: 16 → 0 (critic.py emission 0).
tsc --noEmit: 0 error. next build: 11 routes. eslint: clean.
agent-io-check: agent_io_contract §5 ↔ critic.py / schemas.output (canonical-only) drift 0.
PlanCard.tsx: 0줄. component_map.md: 0줄.
```

## 5. Rollback

- 문서/스키마/테스트는 git revert. CriticEvaluation Optional deprecated 필드 복원 + select_best fallback 복원 +
  generate.py wiring 제거 시 회귀 0 (Phase 9 상태 복귀). eval baseline 은 canonical 만 측정 → 불변.

## 6. 변경 이력

- 2026-05-31: 제안서 작성(meta/proposals) + ADR-034 기반 contracts 반영 + eval 제거 전/후 검증 + 의도 delta (Slice 4).
