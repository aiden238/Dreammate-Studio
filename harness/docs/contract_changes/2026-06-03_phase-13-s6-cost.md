# Contract Change Log — Phase 13 Slice S6 cost_control_policy rich/B-RES-1 cost 재조정 (additive)

> ID: CC-016
> Status: **decided + applied** (2026-06-03, Phase 13 Slice S6)
> Date: 2026-06-03
> Decision: `ai_system/orchestration/cost_control_policy.md` 에 Phase 13 rich 출력(compact→rich) cost 영향 + Phase 11 B안 잔여
>           B-RES-1(다중-provider cost 재조정)을 **additive** 로 정식화한다. ★ **gated default-off** — flag OFF=기존 cost
>           100% 동일(byte-identical 해석), ON=rich cost. 기존 §1~§12(호출당/세션당/일일 상한 + alias 표 + cross_validation)
>           본문 **무수정**.
> Author: Claude (Phase 13 Slice S6)
> Related contracts: `ai_system/orchestration/cost_control_policy.md` §13(rich cost gated) + §14(rich+다중-provider 합산, B-RES-1)
> Related CC: CC-010 (LLM Gateway alias 표 + cross_validation cost, §11~§12), CC-012 (rich 스키마 12 슬롯),
>             CC-013 (rich P-006 v1.1.0), CC-014 (rich gated wiring), CC-015 (Critic depth P-007 v1.2.0)
> Related phase: `phases/archive/phase-13-output-enrichment/` (S6 — cost 재조정 + phase-complete)
> 근거: `eval/regression_results/2026-06-02_phase-12-s2-s3-depth-gap.md` §6 + `eval/regression_results/2026-06-03_phase-13-s6-depth-remeasure.md` §6
> Skill: cost-review (rich/다중-provider cost 영향 분석) + contract-change (절차)

---

## 1. 변경 요약

| 대상 | 변경 | 종류 |
|---|---|---|
| `cost_control_policy.md` 헤더 | 상태 줄에 `§13~§14 rich/B-RES-1(CC-016, Phase 13 S6) additive` 명시 | doc-only |
| `cost_control_policy.md` §13 (신규) | **rich 출력 cost (gated default-off)** — rich = 출력 토큰 compact 대비 **3~5배**(12 슬롯 요구) × 3-plan = ×3안. `rich_output_enabled` OFF=cost 증가 0 / ON=rich 상한. 호출당 상한 표(Planning rich ~$0.009~$0.015 / Critic 9차원 ~$0.007~$0.008, 추정 상향) + tier 정책 rich 활성 조건 1줄(유료/opt-in 권장). | **additive (gated)** |
| `cost_control_policy.md` §14 (신규) | **rich + 다중-provider 동시 ON cost 합산 (B-RES-1 통합)** — provider 단가 5~7배 × rich 토큰 3~5배 = 합산(곱) 십수 배 이상 가능 → 선제 차단 + cost-review 모니터링 필수. 동시 ON = paid+opt-in 한정 권고. default 둘 다 OFF=합산 비용 미발생. | **additive (B-RES-1 흡수)** |

## 2. gated / behavior-preserving 근거

- **§1~§12 무수정**: 기존 호출당(§2)/세션당(§3)/일일(§4) 상한 + 모델 선택(§5) + alias 표(§11) + cross_validation(§12) **전부 보존**. §13~§14 는 추가만(additive).
- **gated default-off ★**: `rich_output_enabled` default **False**(S3 CC-014) → compact 직렬화/프롬프트 → rich cost 증가 0. §2/§3 상한 기존 그대로(byte-identical 해석). S6 깊이 재측정 **OFF 0.231 byte-identical** 재확인과 정합.
- **B-RES-1 통합 ★**: Phase 11 B안 잔여(다중-provider cost 재조정)를 §14 로 흡수 — `multi_provider_plans_enabled` default OFF(PROJECT_STATE) 유지 시 비용 미발생. rich + 다중-provider 동시 ON 합산 주의만 정식화(정밀 단가 재조정은 B안 ADR 후 별도 CC).

## 3. 비용 영향 (gated ON 시 — 추정)

```
compact (OFF, 운영 default)  : Planning $0.003/호출 × 3안, Critic 8차원 $0.006  — 기존 §2 그대로
rich (ON, gated)             : Planning ~$0.009~$0.015/호출 × 3안 (출력 3~5배), Critic 9차원 ~$0.007~$0.008
rich + 다중-provider (둘 다 ON): 위 rich 토큰 × provider 단가 5~7배 = 세션당 십수 배 이상 가능 → 선제 차단 + 모니터링 필수
```

- ★ 추정 상향(데모 토큰 기준) — rich 활성 후 `cost-review` Skill(§8)로 agent_io_logs 실측 분포 점검 + 정밀 재조정 권장.

## 4. tier 정책 (1줄 — §13.4)

- rich 출력 활성 = **유료(paid) / opt-in 권장**. free tier(일 $0.10 §4) 는 compact(OFF) 기본 유지 — rich 토큰 3~5배 × ×3안 이 무료 상한 소진 가속하므로. (§11.1 tier 시그니처 활용, 후속 tier 분기 정식화.)

## 5. 활성화 / Rollback

- **활성화**: env `RICH_OUTPUT_ENABLED=true`(S3/S4 동일 flag). 다중-provider 동시 = `MULTI_PROVIDER_PLANS_ENABLED=true` 추가. flag ON 전환 결정은 S6 깊이 재측정(≥0.8, 실측 1.000 PASS) + cost 모니터링 합의 후.
- **Rollback**: §13~§14 git revert. additive + gated 라 OFF default 면 revert 전후 cost 정책 해석 동일.

## 6. 변경 이력

- 2026-06-03: Phase 13 S6 — rich 출력 cost(§13, gated default-off, 3~5배 × 3안) + rich+다중-provider 합산(§14, B-RES-1 통합) additive. 기존 §1~§12 무수정. flag OFF=cost 불변(byte-identical 해석). 정밀 단가 재조정 = B안 ADR 후 별도 CC.
