# Contract Change Log — Phase 13 Slice 2 P-006 planning 프롬프트 rich 변형 (v1.0.0 → v1.1.0, gated)

> ID: CC-013
> Status: **decided + applied** (2026-06-02, Phase 13 Slice 2)
> Date: 2026-06-02
> Decision: planning P-006 에 rich SYSTEM_PROMPT(v1.1.0) 추가 — S1 rich 슬롯 12종을 채우도록. ★ minor bump (additive 슬롯 + 신규 규칙, output envelope 구조 동일). v1.0.0(compact) 보존, `rich_output_enabled` flag 로 공존.
> Author: Claude (Phase 13 Slice 2)
> Related contracts: `ai_system/prompts/prompt_registry.md` (§7 P-006 — v1.0.0 + **v1.1.0 추가**)
> Related CC: CC-012 (`2026-06-02_phase-13-output-schema-rich.md` — rich 슬롯 스키마, 본 프롬프트가 채우는 대상)
> Related phase: `phases/active/phase-13-output-enrichment/` (acceptance A3)
> Skill: prompt-version-review (semver + 회귀 + gated 활성) + contract-change (registry 절차)

---

## 1. 변경 요약

| 대상 | 변경 | 종류 |
|---|---|---|
| `ai_system/prompts/prompt_registry.md` §7 P-006 | **v1.1.0 rich 블록 추가** (System rich + Output schema rich + Semver gated 정책). v1.0.0 compact 블록 **무변경 보존**. | **additive (minor bump)** |
| `backend/fastapi/agents/planning.py` | `RICH_SYSTEM_PROMPT` 상수 + `_build_rich_system_prompt_with_hint()` 헬퍼 + `RICH_PROMPT_VERSION="v1.1.0"` 추가. 기존 `SYSTEM_PROMPT`(compact)·`_build_system_prompt_with_hint`·`PROMPT_VERSION="v1.0.0"` **무변경**. | **additive** |

## 2. semver 판정 (prompt-version-review §2)

- **minor (v1.0.0 → v1.1.0)** — 새 규칙 + 선택적 출력 필드(rich 슬롯) 추가, output envelope 구조(JSON `{"plan": {...}}`) 유지. major 아님(스키마 호환 — rich 전부 Optional/additive, CC-012). 모델 교체 없음(gpt-4o-mini 동일).

## 3. 코드 영향 (★ behavior-preserving — 런타임 미연결)

```
backend/fastapi/agents/planning.py  — additive:
  + RICH_SYSTEM_PROMPT (rich 12 슬롯 지시 + compact 규칙 계승 + 브리프 경계).
  + _build_rich_system_prompt_with_hint(approach_hint) (3-plan rich 경로용, S3 가 호출).
  + RICH_PROMPT_VERSION = "v1.1.0".
  ★ 기존 SYSTEM_PROMPT / _build_system_prompt_with_hint / run_planning / run_planning_parallel_3 /
    run_planning_multi_provider_3 / PROMPT_VERSION 전부 무변경.
  ★ rich 프롬프트·헬퍼·버전은 S2 시점에 어디에서도 호출되지 않는다 → 런타임 출력 불변(어느 경로도 compact).

backend/fastapi/orchestration · routers · schemas  — 무변경 (rich 프롬프트 선택 분기 = S3 gated wiring).
```

## 4. 회귀 안전 근거 (behavior-preserving)

- **gated 공존 ★**: v1.0.0(compact)·v1.1.0(rich) 이 `rich_output_enabled` flag 로 공존. OFF(default)=v1.0.0
  compact byte-identical, ON(검증 후)=v1.1.0 rich. 표준 deprecate+deactivate 미적용(어느 버전 차단할지 S6 후 결정).
- **런타임 미연결 ★**: rich 상수/헬퍼/버전은 신설이며 호출처 0 (S3 wiring 전). 따라서 모든 생성 경로는 여전히
  compact SYSTEM_PROMPT(v1.0.0) → 출력·동작·토큰·latency 불변.
- **mock 회귀 불변 ★**: eval-run mock-deterministic 러너는 프롬프트를 호출하지 않고 canonical fixture 합성 →
  P-006 프롬프트 변경과 무관하게 동일 결과 (schema 100% / pass 100% / gate PASS).
- **depth 재측정은 S6 ★**: v1.1.0 rich 출력의 depth_actionability(0.231 → ≥0.8)는 실 LLM 필요(CC-011 — mock
  미채점). S3 wiring 후 S6 에서 flag ON 라이브로 측정. S2 는 프롬프트 capability + 버전 등록까지.

## 5. golden_set 회귀 (prompt-version-review §4 — S2 시점)

```
대상: P-006 (planning), v1.0.0 ↔ v1.1.0.
schema 준수율: 100% (v1.1.0 rich 출력도 CC-012 additive Optional → output_schema §8.1 v1.2.0 valid).
런타임 영향: 0 — rich 프롬프트 미연결(S3 전)이라 어느 케이스도 compact 경로 → 신구 출력 차이 측정 불가(=불변).
pytest: 481 → 481 + 신규 (기존 수정 0). 
★ rich ↔ compact 실측 대조(depth/토큰/latency)는 S3 wiring 후 S6 에서 실 LLM 로 수행 (eval/regression_results/phase-13-*).
상세: eval/regression_results/P-006_v1.1.0.md
```

## 6. 활성화 / 모니터링 (gated — 표준 절차 변형)

- **활성화**: 표준 patch/minor 즉시 active 대신, **gated** — v1.1.0 은 `rich_output_enabled` ON 경로 전용.
  flag default OFF(S3) → 운영 기본은 v1.0.0. flag ON 전환은 S6 depth 재측정(≥0.8) + 라이브 검증 후 별도 결정.
- **모니터링**: flag ON 전환 시점(S6+)부터 schema 파싱 실패율 / Critic 점수 / 토큰(cost-review) / feedback.
- **이전 버전**: v1.0.0 deactivate_at 미설정 (gated 공존 — 차단 안 함).

## 7. Rollback

- `prompt_registry.md`: §7 v1.1.0 rich 블록 + Semver gated 단락 git revert → v1.0.0 단독 복귀.
- `planning.py`: RICH_SYSTEM_PROMPT / _build_rich_system_prompt_with_hint / RICH_PROMPT_VERSION 제거.
- additive + 미연결이라 rollback 시 런타임 영향 0 (어차피 compact 경로만 동작 중).

## 8. 변경 이력

- 2026-06-02: Phase 13 S2 — P-006 v1.0.0 → v1.1.0 (rich, minor, gated). planning.py rich 상수/헬퍼/버전
  additive(미연결) + prompt_registry §7 v1.1.0 블록. 근거: Phase 12 깊이 격차 + CC-012 rich 스키마.
  다음: S3 (config `rich_output_enabled` default False + generate/orchestrator gated 분기 — OFF=compact v1.0.0
  byte-identical via `Plan.model_dump_compact()`, ON=rich v1.1.0 프롬프트+스키마).
