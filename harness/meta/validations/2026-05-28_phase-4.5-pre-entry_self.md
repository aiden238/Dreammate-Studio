# Phase 4.5 Pre-Entry Multi-LLM Validation — Self (Claude Code)

> 검증 모델: Claude Code (자가, 지침 참조)
> 검증 일자: 2026-05-28
> 검증 유형: formal (skill_usage_log.md에 첫 formal 기록)
> 외부 검증: `2026-05-28_phase-4.5-pre-entry_external.md` (별도 placeholder)

## 검증 대상

1. Critic revise loop 설계 (최대 2회, max attempts 차단)
2. Rewriter 인라인 prompt 패턴 (prompt_registry 정식화 전 단계)
3. Z-X3 best-plan selection (8-dim 평균 + tie-breaking plan_index 작은 쪽)
4. P-X2 phase-complete 자동 게이트 (scenario_simulation.ps1)

## 참조한 지침

- `harness/CLAUDE.md` § "검토 유형별 참조" (AI 구조, 평가 체계, 메타 개선)
- `harness/AGENTS.md` (구현/QA 모델 라우터)
- `harness/docs/contracts/agent_io_contract.md`
- `harness/docs/contracts/output_schema.md`
- `harness/docs/contracts/mvp_non_goals.md`
- `harness/eval/golden_set.md` (FC-001~005, 참조만)
- `harness/meta/patterns.md` (P-X1, P-DRIFT-001, P-GPT-REVIEW-001, P-GRACEFUL-001)
- Phase 4 archive `closing_notes.md` (revise loop 이관 사유)
- Phase 4 회고 proposals (Z-X1~Z-X3 + P-X2 채택 권장)
- Phase 4.5 entry files (goals/scope/non_goals/assumptions/multi_slice_plan)

## 검증 결과 (4 항목)

### V1. Critic revise loop 설계 — PASS

- 최대 2회 차단 → 무한 루프 위험 0 (확정 결정 [5] 정합)
- graceful: revise 실패 시 원본 plan 유지 + validation.warnings 추가 → P-GRACEFUL-001 정합
- revise_history는 Optional 필드 추가 → output_schema 회귀 0 (contract-change 절차 불필요 — Optional 필드 추가는 호환 변경)
- 잠재 risk: revise 응답이 원본보다 나쁜 경우 (regression) — Phase 4.5는 본 risk 미해결 (NG8 eval 정식화는 Phase 9+)
- 권장: Phase 9+ eval-run Skill 정식화 시 revise effect 측정 필수

### V2. Rewriter 인라인 prompt 패턴 — PASS (조건부)

- Phase 6+ prompt_registry 정식화 전까지 인라인 허용 (NG7 정합)
- 인라인 prompt는 `agents/rewriter.py` 모듈 상수로 명시 (function 내부 string concat 금지)
- 잠재 risk: prompt drift (다른 곳에 동일 prompt 복제 시) — Phase 4.5 단일 함수만 사용하므로 risk 낮음
- llm_security_contract 정합: PII 마스킹 / 인젝션 차단은 기존 intent.py 패턴 재사용

### V3. Z-X3 best-plan selection — PASS

- 8-dim 평균 점수 + tie-breaking plan_index 작은 쪽 → 결정성(deterministic) 보장
- recommended_plan_index는 Optional[int] → 회귀 0
- frontend wrapper UI 방식 → PlanCard.tsx 무수정 (사용자 결정 6-a 정신 계승)
- 잠재 risk: 8-dim 가중치 동일 가정 — 추후 dim별 가중치 분리 가능성 (Phase 9+ eval로 보정)

### V4. P-X2 phase-complete 자동 게이트 — PASS

- scenario_simulation.ps1이 phase-complete §1.6에서 자동 호출 → manual walkthrough 제거
- 시나리오 1~5 (Phase 3 변경성 5개) 자동 재현 → 회귀 검출 일관성 ↑
- 잠재 risk: 시나리오 1~5가 Phase 5+ DB 도입 시 부분 무효화 가능 — phase-complete가 시나리오 추가/수정 가능하도록 config 분리 권장 (Phase 5 진입 직전)

## 종합 판정

**Phase 4.5 entry 허용 — 4/4 PASS (V1~V4)**

다음 단계: Slice 1 P-X2 채택 작업 진행 (이 self-validation 문서 작성 후 commit).

## 외부 검증 연계

- 본 self-validation은 단일 모델 (Claude Code) 결과
- 외부 (GPT/Gemini) 검증 결과는 `2026-05-28_phase-4.5-pre-entry_external.md`에 사용자가 별도 진행 후 추가
- 두 결과의 차이 항목 발견 시 Phase 4.5 진행 중 `notes.md`에 기록 + Slice 4 회고 §개선 제안 반영
