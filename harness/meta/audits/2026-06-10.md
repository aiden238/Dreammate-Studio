# harness-audit 결과 — 2026-06-10 (Phase 2 하네스 점검)

> 트리거: 사용자 "Phase 2 하네스 점검" + 이번 세션(2026-06-09~10) 발견 체계화.
> 절차: harness-audit v1.2.0 (§1~§8). 자동검사 `Temp/harness_audit_checks.py` + `scripts/audit_naming.ps1`.
> 범위: 하네스 전체 (skills 21 / docs/contracts / instruction_index / meta).
> ★ proposal-only — 본 audit는 직접 변경 0. 실 변경은 contract-change/사용자 결정.

## 자동 검사 (결정론, LLM 0)
- §2 stub(<30줄, marker 없음): **0** ✅
- §5 instruction_index(routes.yaml + catalog.yaml target 실존): **0 깨짐** ✅
- §6.5 audit_naming(contract 명명 일관성): **PASS, 0 drift** ✅
- SKILL related_* 경로: **1** (rag-update → `knowledge/candidate_knowledge/` = Supabase 테이블, 디렉토리 아님)

## §7 분류

### critical (0) — 없음 (구조 무결성 양호)

### high (0) — 없음

### medium (4)
1. **routes.yaml ↔ skill 이중라우팅 미연결**: routes.yaml(task_type→문서 로딩, context eng)과 skill 키워드 트리거(절차)가 병존·미연결. 실사용에서 phase-start가 routes.yaml phase_start 흡수 → routes.yaml 점차 장식화. → 의도된 2-track으로 명문화 or 정리. [후속: contract-change + INDEX 갱신]
2. **default-off "죽은 게이트" 4개** (A7 flag audit):
   - `cross_validation_enabled`: wired but **logging-only(Envelope 0 변경)** → 의사결정 승격 or sunset 결정.
   - `critic_calibration_enabled`: 가치 **高**(88점 함정 보정)인데 미활성 → 측정 후 활성 검토.
   - `multi_provider_plans_enabled`: A11 측정=**다양성 real(0.959→0.680) but 취약**(provider 가용성) → Claude JSON robustness + provider 안정 선결.
   - `agent_io_log_to_db`: 선택 sub-flag → 유지.
   [후속: 사용자 결정 + 각 측정]
3. **skill 통합/격하 후보** (skill 실효 audit, 11실사용/6잠재/4dead): eval-design+eval-run 통합 / rag-design+rag-update 통합 / phase-review·context-compact 격하 / **bug-triage 강제 게이트화**(실 버그수정이 절차 우회 중=위험). [후속: contract-change(skill)]
4. **검토형 skill 산출물 contract drift**: security-review(3트리거/산출물 0)·design-review(10트리거/산출물 0)가 SKILL.md 의무 경로(`eval/security_reviews`·`eval/design_reviews`) 비움 = 계약 위반 묵인. [후속: contract-change(산출물 요구 완화 or 강제)]

### low (1)
1. rag-update SKILL `related 'knowledge/candidate_knowledge/'` = Supabase 테이블(디렉토리 아님) → frontmatter 경로 표기 정정.

## ✅ 이번 세션 자정작용 (하네스가 하네스를 개선 — 기록)
- **A8 `scripts/skill_usage_report.py`**: skill_usage_log 수동·stale(5/31~) → **코드 자동집계로 졸업**(cost_report 모델). 6개월폐기·3회자동화 룰의 데이터원 복구.
- **경계 강화**: `BOUNDARIES.md` + `.github/CODEOWNERS` + `scripts/check_boundaries.py`(L1/L2 기계 가시화, --strict CI 연결 가능).
- (이전) cost-review → `cost_report.py` 자동화 졸업.

## §8 종료
**critical 0 / high 0 → PASS (구조 무결성).** medium 4 = 개선 proposal(사용자 결정·contract-change 대상). 직접 변경 0(proposal-first).
- 후속 매핑: medium 1·3·4 → contract-change / medium 2 → 사용자 결정+측정 / low 1 → contract-change(경미).
- 상세 proposal: `meta/harness_improvement_proposals.md` (2026-06-10 append).
