# Handoff — 2026-06-10 → 다음 세션 (studio project-2 등)

> 목적: 2026-06-09~10 세션이 바꾼 구조를 다음 세션이 빠르게 파악. 진입 시 이 파일 + `PROJECT_STATE.md §🟢🟢 최신 세션` 먼저 읽기.

## 1. 먼저 읽을 것 (순서)
1. `PROJECT_STATE.md` → §🟢🟢 최신 세션 (2026-06-09~10)
2. `product/platform_evolution.md` — 전략 방향: MVP→플랫폼, **임베딩=Gemini/생성=GPT**, 미래 오케스트레이터(§2.4), RAG 튜닝·임베딩 측정(§3.1), 2nd Brain 명문화(§2.5/2.6)
3. `phases/active/phase-27-mvp-realuse-close/notes.md` — A1~A11 상태
4. `meta/audits/2026-06-10.md` — 하네스 audit + HIP-A~D

## 2. 바뀐 것 (코드/설정)
- **RAG 임베딩 = `gemini-embedding-2` @1536** (taskType 비대칭). `config.rag_embedding_provider`(gated, default openai) + `config.rag_embedding_gemini_model` + `embedding.py::_embed_gemini`. `.env RAG_EMBEDDING_PROVIDER=gemini` 로 활성. 생성은 GPT 그대로(분리).
- `agents/intent.py`·`agents/rewriter.py` 에 agent_io 텔레메트리 추가(planning/critic 패턴). `config` realuse_flags 에 `agent_io_log_enabled` 추가.
- 신규 파일: `BOUNDARIES.md`, `.github/CODEOWNERS`, `scripts/check_boundaries.py`, `scripts/skill_usage_report.py`, `product/platform_evolution.md`, `knowledge/rag/seed/2026-06-10_*.md`, `meta/skill_usage_report_generated.md`, `meta/audits/2026-06-10.md`.
- Supabase: `approved_knowledge` 8건(external_seed, **Gemini 임베딩**) 적재됨.

## 3. ⚠️ 하지 말 것
- **RAG 임베딩을 OpenAI로 되돌리지 말 것** — `approved_knowledge` 가 Gemini 임베딩이라 query를 OpenAI로 바꾸면 공간 불일치(검색 garbage). 바꾸려면 전체 재임베딩.
- Gemini 크레딧 0이면 RAG graceful-off(앱 정상, GPT 생성 무영향) — `ai.studio/pro` 크레딧 확인. (충전 완료 상태.)
- gated default-off 철학 유지: 신규 동작은 flag 뒤 additive + "OFF면 byte-identical" 회귀.

## 4. 이어서 할 일 (main 제품 작업)
- Phase 27 realuse-마감 미완: **A2 AppShell 네비 → A5 첫-사용자 e2e → A3 rate_limit**.
- (별도) 하네스 proposal HIP-A~D = contract-change/사용자 결정 후.

## 5. 검증 상태
- `pytest 812 green` (rootdir `harness/` 기준; backend/fastapi 에서 돌리면 실 .env 누수로 일부 실패 — 알려진 비-hermetic 이슈, chip 분리됨).
- RAG: `retrieval.search()` @0.7 = 3/2/1 hits (Gemini, 크레딧 충전 후 확인).
- 재현 스크립트: `Temp/`(gitignored) — rag_gemini_adopt / rag_embed_measure / a7_flag_audit / a11_provider_diversity / pkm_http_jwt_e2e.
