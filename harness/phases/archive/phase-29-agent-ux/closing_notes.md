# Phase 29 — Closing Notes

- 종료일: 2026-06-17
- 상태: **done** (S0~S5 + 멀티모달(A) + 동적 그래프(B1) main 머지, HEAD `aa2d236`)

## 목표 (goals.md 요지)

"기능이 없는 게 아니라 사용자가 기능까지 도달 못 하는 게 문제" — 랜딩·진입·용어·에이전트 느낌을 닫아, 첫 사용자가 도움 없이 핵심 루프(아이디어→상황→질문→기획안→저장→내 brain)를 완주. 신규 제품기능 0, 경험층 마감.

## acceptance 판정

| 슬라이스 | 상태 | 근거 |
|---|---|---|
| S0 Discovery CTA 가림 버그 | ✅ | AppShell HIDDEN_PREFIXES 에 `/new`·`/plan` 추가(집중 플로우 네비 숨김), 홈·/brain 유지. (`7ffc0c2`) |
| S1 랜딩 + 4 상황 버튼 | ✅ | 슬로건 + 큰 입력창 + 4 상황 버튼(아이디어 정리/브랜드 방향/SNS 반응/바로 기획안) + 흐름 표시. 기존 route 재사용. (`7ffc0c2`) |
| S2 용어 번역(jargon 제거) | ✅ | Discovery 헤더·step1·직접입력 표시 문자열 평이화. 내부 코드/스키마 불변. (`7ffc0c2`) |
| S3 진행 요약 패널 | ✅ | 질문 흐름에 "현재 정리된 방향" 누적 표시. (`dd833f6`) |
| S4 저장 후 Brain CTA + 네비 보강 | ✅ | 저장 후 [내 brain에서 보기]/[같은 방향 새 영상]/[SNS(준비중)]. (`dd833f6`) |
| S5 에이전트 느낌 | ✅ | 생성 진행 메시지 + "내 brain 반영됨" + Phase 28 S3(내 컨셉 수렴) 결합. (`dd833f6`) |

## 추가 슬라이스 (S0~S5 외 — 본 phase 내 확장)

| 항목 | 상태 | 근거 |
|---|---|---|
| 4 상황 버튼 distinct — branding goal 분기(SNS/정리) | ✅ | `db0e6bf` |
| 프롬프트 입력 카드 폴리시 + 멀티모달 첨부 affordance(준비중) | ✅ | `8a2ed32` |
| A 멀티모달 — 레퍼런스 이미지 분석 → 생성 반영(A1) + 키워드 PKM(A2) | ✅ | `2c738a7` |
| A2 키워드 PKM 적재를 plans 영속 이후로 이동 (FK 23503 회귀 fix) | ✅ | `18795d6` |
| B1 /brain 동적 force 그래프 (react-flow + d3-force) | ✅ | `c7b5530` |

## 이월

- 멀티모달 "준비중" affordance 중 미연결 항목의 실연결.
- 첫 사용자 무도움 완주 라이브 e2e(브라우저)의 사용자 실검증 — headless 한계로 데모/유닛 대체.
- 범위 밖(goals): SNS 자동완성·니즈서치·공유·협업·트렌드·영상제작/편집/업로드 → 1차 검증 후.

## 강제 종료 사유

없음 — S0~S5 + 추가 슬라이스 전부 머지·동작. 신규 제품기능 0 원칙 유지(경험층 마감 + 멀티모달/그래프는 기존 데이터·route 위 additive).
