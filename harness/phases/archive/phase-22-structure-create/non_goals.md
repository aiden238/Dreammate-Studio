# Phase 22 — Non-Goals

```
- domains/series migration 금지 — 테이블 이미 존재(0001). 본 phase 는 create 메서드+endpoint+UI 만.
- video_projects 생성/노드 미포함 — 4계층 중 Domain·Series 까지. Video 는 후속.
- domain/series 편집·삭제 미포함 — 본 phase 는 **생성**만(편집/삭제는 후속, pkm 큐레이션과 별개).
- 위저드(/new/discovery) 의 domain/series 자동 생성 연결 미포함 — /brain 직접 생성만(위저드 연결은 별도).
- 개인 PKM 출처 migration(Phase 21 이월) 미포함.
- 신규 데이터모델 0 / 그래프 집계 로직 변경 0(Phase 21 그대로 — 생성된 데이터가 자동 반영).
```
