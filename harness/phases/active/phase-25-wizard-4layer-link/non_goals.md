# Phase 25 — Non-Goals

```
- ★ 범용 Discovery/Quick 위저드 step 의 domain/series 캡처(위저드 재설계) 금지 — 현 위저드는 domain/series discrete 선택 미보유. 본 phase 는 브랜딩 세션 택1 → 자동 시드만.
- video 노드 / video_projects 생성 미포함(4계층 중 domain·series 까지).
- 개인 PKM 출처 migration 미포함(별건).
- domain/series migration 금지 — 테이블 존재(0001).
- 매 generate 마다 domain 생성 금지 — initial_input 자유텍스트는 noisy. 큐레이션된 branding topic 만.
- 기존 branding select 의 PKM 시드(Phase 18) 변경 금지 — domain/series 시드는 additive 추가.
- 신규 데이터모델 0 / 그래프 집계 변경 0(자동 생성 데이터는 기존 read 가 반영).
```
