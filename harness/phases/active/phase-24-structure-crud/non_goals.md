# Phase 24 — Non-Goals

```
- 위저드↔4계층 자동 연결 금지 — 별도 phase(🅑 나머지). 본 phase 는 /brain 직접 편집·삭제만.
- video 노드 / video_projects CRUD 미포함 — 4계층 중 domain·series 까지.
- 개인 PKM 출처 migration 미포함(Phase 21 이월).
- domain/series migration 금지 — 테이블 존재(0001). 메서드+endpoint+UI 만.
- PKM(개인/브랜드) 편집·삭제는 이미 됨(Phase 19 S4) — 중복 금지.
- 신규 데이터모델 0 / 그래프 집계 로직 변경 0(편집·삭제는 기존 read 가 자동 반영).
- 일괄/대량 삭제·복구(undo) 미포함 — 단건 편집/삭제만.
```
